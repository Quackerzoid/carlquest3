# CLAUDE.md — Carl Quest Sports

Multiplayer (2-player) 3D rounders game. TypeScript strict, Three.js, Rapier physics, Colyseus authoritative server, Vite. Monorepo: `/client`, `/server`, `/shared`. The design spec at `docs/design/spec.md` is the single source of intent — implement it exactly as written.

## 1. NON-NEGOTIABLES

- **Use the `superpowers` plugin.** Its skills define the workflow below; invoke the named skill at each stage. Do not freelance around it.
- **Plan before executing. Always.** No code, no file edits, no refactors without a written plan first (see §2, steps 1–2). Trivial one-line fixes still get a one-paragraph plan stating the change and how it will be verified.
- **Use sub-agents for implementation whenever possible.** Delegate independent tasks to sub-agents rather than doing everything in the main session; parallelise independent work (see §2, step 4).
- **Server is authoritative.** All game logic and physics live in `/server`; the client only renders and predicts. Every network message is phase-validated server-side.
- **All tunables in `/shared/constants.ts`; all stat formulas as pure functions in `/shared/formulas.ts`.** No magic numbers elsewhere. `characters.ts` is the only roster data source.
- Fixed 1/60 s deterministic server physics step. British English throughout.
- **Use the `unslop-ui` skill when designing any UI element** (lobby, draft screen, positioning panel, HUD, result screen). Make a deliberate, project-specific design choice rather than shipping templated defaults or an AI-generated look.
- **Maintain the Project Log (§6) in this file.** After every completed task, and before ending any session, WRITE what was done to §6. At the start of every session, READ §6 before doing anything else and treat it — not your memory of prior sessions — as the ground truth of project state. If §6 and your assumptions disagree, §6 wins; if §6 and the actual code disagree, inspect the code, then correct §6.

## 2. THE WORKFLOW, SKILL BY SKILL

Follow this lifecycle for every feature, fix, or change. Each named skill is invoked at its stage.

1. **Brainstorm first (`brainstorming`).** Before ANY creative or implementation work, on any new feature or behaviour change, use `brainstorming` to explore intent, requirements and design. For this project the design documents in `/docs/design/` are the primary intent source; use brainstorming to reconcile the spec with the existing code and surface unknowns (ambiguous rules, untuned constants, missing acceptance criteria) before writing anything. Never jump straight to code.

2. **Write a plan (`writing-plans`).** Once the design is clear, use `writing-plans` to produce a written implementation plan before touching code. Every non-trivial module or milestone gets a plan: files to create/change, interfaces, tests to write first, and the verification command that proves it done.

3. **Isolate the work (`using-git-worktrees`).** Before executing a plan or starting feature work that needs isolation, use `using-git-worktrees` so the work happens in a clean, separate workspace. One worktree per milestone or major module.

4. **Execute with TDD and sub-agents.**
   - Use **`test-driven-development`** when implementing any feature or bugfix: write the test/verification first, then the implementation. For this project that means defining the failing Vitest case first — the formula output, the state-machine transition, the validation rejection, the ability modifier, the physics deviation (tolerance-based) — before writing the module, then building until it passes.
   - Use **`subagent-driven-development`** to execute the plan's independent tasks in the current session, and **`dispatching-parallel-agents`** whenever there are 2+ independent tasks with no shared state or sequential dependency. Default to parallelising: one agent per independent module (PhysicsModule, PitchModule, HitModule, FieldingModule, RunningModule, RulesModule, PositioningModule, DraftModule), a dedicated client/UI agent, a dedicated test agent. The `/shared` contracts (types, constants, formulas) must land first, then the server modules are largely independent and well suited to heavy parallel dispatch.
   - Use **`executing-plans`** when running a written plan across review checkpoints.

5. **Debug systematically (`systematic-debugging`).** On ANY bug, test failure, or unexpected behaviour — including desyncs between client render and server state, physics non-determinism, or Colyseus schema patch errors — use `systematic-debugging` BEFORE proposing a fix. No guess-and-check patching. Reproduce with a minimal deterministic test (fixed seed, fixed timestep), diagnose the actual cause from logs/state dumps, then fix the root, not the symptom.

6. **Verify before claiming done (`verification-before-completion`).** Before you call anything complete, fixed, or passing, before any commit or merge, use `verification-before-completion`: run the actual verification and confirm the output. Evidence before assertions, always. "It should work" and "it compiles" are not completion. For this project, completion means: `npm run check` (typecheck + lint + Vitest) passes clean across all workspaces; the milestone's acceptance behaviour was demonstrated in a real run (two clients over Colyseus for networked features); out-of-phase and illegal messages are shown to be rejected server-side for any networking/validation change; and no `any`/`@ts-ignore` was introduced without a justifying comment. Once verified, immediately record the result in the Project Log (§6) — verification without a log entry is incomplete.

7. **Request review (`requesting-code-review`).** On completing a major feature or before merging, use `requesting-code-review` to verify the work meets requirements. If you receive review feedback, use **`receiving-code-review`**: verify each point with technical rigour rather than performative agreement or blind implementation; push back where a suggestion is wrong, confirm where it is right.

8. **Finish the branch (`finishing-a-development-branch`).** When implementation is complete and all verification passes, use `finishing-a-development-branch` to decide integration (merge, PR, or cleanup) properly rather than leaving work dangling.

## 3. PROJECT COMMANDS

- `npm run dev` — client (Vite) + server (Colyseus) in watch mode.
- `npm run check` — typecheck + lint + test, all workspaces. Must be green before any commit.
- `npm run test` — Vitest.
- `npm run build` — production build of client and server.

## 4. ORDER OF WORK

Follow the milestone order in `docs/design/spec.md` §9 strictly; each milestone must be runnable, verified, and committed (tagged) before the next begins. Within a milestone, land `/shared` contracts before dispatching parallel server-module agents.

## 5. WHEN THE SPEC IS SILENT

Use the defaults in spec §8b without asking. For anything else genuinely ambiguous, surface it during `brainstorming` and ask the user — do not invent rules of rounders or retune constants unprompted. Tuning suggestions go in a `TUNING.md` note, not into code.

## 6. PROJECT LOG — LIVE STATE (Claude Code MUST maintain this)

This section is the anti-hallucination ledger. It is the **only** trusted record of what exists, what works, and what was decided. Never claim a module, test, file, or feature exists unless it is recorded here or you have just verified it in the actual codebase. Never rely on memory of previous sessions.

**Rules:**
- **Write, don't summarise from memory.** Update this section immediately after each completed task — not at the end of a long batch. Sub-agents report back; the main session records their results here after verifying them.
- **Only log verified facts.** An entry may only say "done/passing" if `verification-before-completion` was run and the command output confirmed it. Log the command used.
- **Log decisions and deviations.** Any interpretation of an ambiguous spec point, any deviation from the spec (with user approval noted), any constant tuned away from its §6 suggested value.
- **Log known issues honestly.** Failing tests, flaky behaviour, TODOs, and anything skipped go in Known Issues — never silently drop them.
- **Keep entries terse and factual.** Date, milestone/task, files touched, verification evidence. No prose padding.
- Update **Current State** (overwrite in place) and append to **Changelog** (never rewrite history).

### 6.1 Current State (overwrite to reflect reality)

- **AUTO-PLAY REDESIGN (2026-07-05, USER-DIRECTED, whole-stack) — Tasks 1–7 COMPLETE on worktree branch `worktree-autoplay-redesign`, pending final whole-branch review + merge.** The game loop is REDESIGNED: players no longer send ANY play message — every pitch, swing, run decision and catch resolves as an automated server dice beat, broadcast to both clients as a `roll` event (`RollEvent {contest, actorId, detail, roll, threshold, success}`) and presented as a flashed roll banner + feed line. **Spec §7's player play-messages (pitch/swing/runDecision) are SUPERSEDED (user-directed):** the handlers stay registered as tombstones rejecting with exactly `'plays resolve automatically'` (paused-first ordering kept). The field ORIENTATION is x-MIRRORED (POSTS + FIELDING_POSITIONS): post 1 now at x = −11 so runners visibly circle counter-clockwise from the match camera (symmetry-preserving — no formula or physics change). Presentation identity replaced: batter view with bat prop + wind-up/swing animations; clamped orbit camera (drag orbit, wheel zoom, Home/double-click reset) + procedural skybox/lighting pass; broadcast lower-thirds esports UI restyle (dark glass slabs, corner cuts, team-slash bars, roll banners; parchment retired). Delivered per task: **T1 (283ea75)** shared contracts — mirrored FIELD geometry, `RollEvent`, `AUTOPLAY_*` constants; **T2 (8113eaf)** pure `AutoPlayModule` (`pitchDecision`/`swingDecision`/`runDecision`, stat-weighted draws, documented per-decision draw counts); **T3 (10310b4)** MatchRoom auto-beats (sim-time pitch beat incl. the no-contact respawn re-schedule; pre-sampled swing timing error applied through the REAL `resolveSwing` window chain at the plane crossing — presentation can never contradict resolution; run beats at contact + post arrivals via `running.setDecision`, rate-limited broadcasts), FieldingModule `onRoll` dep, tombstones, `roll` broadcasts, 38-row test migration (43 room tests; WALL flake dead — root-caused to the removed real-time swing seam) + a REAL room defect found and fixed (stale schema ball at play boundaries — `syncBall()` after every boundary `spawnBall()`); **T4 (5b362a9)** client `BatterView` (bat prop, wind-up + swing animations, kit-aware, runner-suppressed); **T5 (100058d)** `CameraControls` (clamped orbit + zoom + Home reset, drag/click suppression) + skybox; **T6 (ce176ea)** UI restyle + `UI.showRoll`/`describeRoll` banners (max 2 stacked, 1.4 s flash, feed echo) + full wiring (NetModule `onRoll`, play senders/keys DELETED — Enter/N remain, PositioningControls `isDragging` guard); **T7 (this commit)** acceptance + docs. Design spec `docs/superpowers/specs/2026-07-05-autoplay-redesign-design.md` (322c22a, USER-APPROVED); plan `docs/superpowers/plans/2026-07-05-autoplay-redesign.md` (46c8224); SDD ledger `.superpowers/sdd/progress.md` (gitignored) — every task subagent-reviewed Approved. **T4 incident (logged honestly):** the T4 agent edited the MAIN repo checkout by mistake; the changes were moved into this worktree, re-verified (typecheck + eslint), and the main repo restored clean before commit 5b362a9.
- **Post-ship visual overhaul (2026-07-05, client-only) — COMPLETE, MERGED and TAGGED `visual-overhaul` (main at ba56fec):** the placeholder capsule look is replaced end-to-end. **T1 (d71ddf3)** `client/src/CharacterModels.ts` — NEW: 11 distinct procedural low-poly humanoid rigs (head/neck/torso/pelvis/pivoted arms+legs — the design spec's NO-PILLS rule), driven by a hand-tuned per-character `VISUALS` table (the single looks-source: whale a 3.1 m rounded giant with tiny arms, joe 1.3 m scrawny in an oversized shirt, laurie's ×1.4 arms, robbie's huge forearms/boots, jonty squat+headband, kian's flat cap, josh's keeper gloves, joel barrel-chested rolled sleeves, darcy wristbands, ricy tidy trim, carl gold captain's armband) + `KIT_COLOURS` (A navy 0x22345e / B maroon 0x7a2231, shared cream trim; neutral pre-draft grey). **T2 (d818092)** `SceneModule.ts` — the small stadium: terraced stands, 3,000-cone instanced crowd, 4 floodlight masts, hoardings, mow-striped grass with all chalk drawn from CONST.FIELD, warm gradient sky dome + haze fog; static scene 47,740 tris measured (renderer.info). **T3 (38f74d5)** `RenderModule.ts` (+4-line `main.ts` setTeams wiring) — rigs consumed by both views: per-side kits after the draft, holder = gold feet ring + in-hand ball + cocked arm, selected = cream pulsing ring, out = topple + red tint (all M10 retention/revival semantics preserved), time-based per-frame walk/lean/breathing animation, recursive-raycast picking; whole scene with all 11 rigs 64,612 tris (≤ ~75k target). **T4 (this commit)** browser acceptance `docs/superpowers/acceptance/visual-acceptance.{mjs,txt}` + `visual-01/02/03-*.png`, README gallery swap, this log. **ZERO server/shared change** — verified by `git diff main --stat`: only the four client files (+ docs). Evidence highlights: anti-pill ratio 2.26 (whale 52 px vs joe 23 px at near-equal depth, both drafted onto side B through the real UI), holder gold ring captured live (321 exact-emissive px around kian's feet), picking regression green with a SINGLE ground click (the m8 7-click burst is obsolete — M10's per-frame lerp), zero console errors. Design spec `docs/superpowers/specs/2026-07-04-visual-overhaul-design.md` (128ec16, USER-APPROVED); plan `docs/superpowers/plans/2026-07-04-visual-overhaul.md` (369c093); SDD ledger `.superpowers/sdd/progress.md`.
- **Milestone:** 10 (UIModule polish — lobby, HUD, result screen, rematch; spec §9.10, the FINAL milestone) — **COMPLETE, MERGED and TAGGED**: main fast-forwarded 79f4841 → 670d85e, tagged `m10-ui-polish`, main + tag pushed to origin 2026-07-04. Final whole-branch review verdict "With fixes" — its Important (the client PRESSURE badge covered only the final innings SLOT while the server's pressure window is the whole final PAIR — a plan-formula defect, corrected to exact `RulesModule.pressure()` parity) plus two minors (LEAVE-during-reconnect race guard; reconnection token cleared on markLeaving) were fixed pre-merge in polish commit 670d85e (357/357). Worktree + branch removed. **THE PROJECT'S SPEC §9 BUILD ORDER IS COMPLETE — all ten milestones built, merged, tagged and pushed. Remaining work is post-ship: playtest tuning (TUNING.md candidates), the §6.4 known-issue backlog (seed-option gating hardening, thrown-ball caught classification, roster growth), and content.** The shipped surface is a complete 2-player online rounders game: create/join by 4-letter code through the restyled scorer's-card lobby; alternating character draft with bowler nomination; fielder repositioning, substitutions, batting-order control and the cross-play stamina ledger; the full rules engine (innings pairs, half-rounder scoring, multi-runner running with forced-on cascades, sudden-death tiebreak) with every §3 ability wired into pitch/hit/fielding/physics; an in-match HUD (scorer's board + event feed + contextual key legend), out-runner topple presentation, and a GAME_OVER result overlay with click-to-rematch/leave; plus client-side one-attempt auto-reconnect over the server's pause/grace machinery. Design spec: `docs/superpowers/specs/2026-07-04-m10-ui-polish-design.md` (dfc7545, USER-APPROVED); plan: `docs/superpowers/plans/2026-07-04-milestone-10-ui-polish.md` (79f4841). Commits: T1 NetModule reconnect surface (onUnexpectedDisconnect/tryReconnect/markLeaving) + InputModule paused key gating f35d6b8; T2 self-animating render views + markOut out-runner presentation e60795b; T3 UIModule HUD + main.ts single-teardown orchestration + reconnect flow + lobby restyle + DraftScreen now-batting/parked rows (folding the T1/T2 review follow-ups: onError single-fire path, dying-runner revival guards) 722f96e; T4 acceptance + docs (this commit — no src changes). Each task subagent-reviewed Approved (SDD ledger: `.superpowers/sdd/progress.md`, gitignored). M10 delivered (client-only; server + shared untouched): see the M10 deltas in the Modules bullet. **Previous milestone 9** (wire abilities §3 into their modules, spec §9.9) — COMPLETE, MERGED and TAGGED: main fast-forwarded 5a27a79 → 5434588, tagged `m9-abilities`, main + tag pushed to origin 2026-07-04. Final whole-branch review verdict "With fixes" — its Important (the WALL blocker ate the whale's OWN throws: release point inside his own capsule, empirically probed at 0.32 m pin) was fixed at 5434588 via a flight-start blocker exemption (granted only at placeBall/applyHit when the ball starts inside the capsule; one-way re-arm on footprint exit + 0.05 m hysteresis; never granted mid-flight — a chasing whale still stops the ball dead) plus the opportunistic minors (pitch facts captured at handlePitch; shared frozen NEUTRAL_* mods replacing three local copies); re-review verdict "Ready to merge? Yes" (357/357). Worktree + branch removed. **No next milestone — the spec §9 build order is complete; remaining work is the M10 final review/merge/tag and any post-ship tuning (TUNING.md).** Design spec: `docs/superpowers/specs/2026-07-04-m9-abilities-design.md` (9c3c30d, USER-APPROVED); plan: `docs/superpowers/plans/2026-07-04-milestone-9-abilities.md` (5a27a79). Commits: T1 shared abilities registry + ABILITY constants + spinReadPenalty c04f2e4; T2 pitch ability mods + Magnus curve-onset gating 5f731b6; T3 ability-aware SwingContext (CLUTCH, power base, CANNON window, spin-read) 7f8d460; T4 fielding abilities (IMMOVABLE, BUTTERFINGERS, LONG_REACH, POWERHOUSE, QUICK_DRAW) incl. `fumbledFlight` guard 5a1f3f3; T5 room ability-context threading + WALL blocker stop-dead 4af4518; T6 acceptance + docs (this commit). Each task subagent-reviewed Approved (SDD ledger: `.superpowers/sdd/progress.md`, gitignored); T6 completed across a process crash (acceptance ran and passed pre-crash; docs+commit resumed post-crash). M9 delivered: `/shared` `abilities.ts` registry (`fieldingAbilityParams`/`pitchAbilityMods`/`hitAbilityMods` derived from each Character's AbilityId), `CONST.ABILITY` tunables (spec-literal except invented `SPIN_READ_W`), `spinReadPenalty` formula, `PitchParams.curveOnsetS`; PitchModule ability mods + PhysicsModule Magnus onset gate (CURVEBALL_MASTER curve activates at `CURVE_ONSET_FRACTION` of flight); HitModule `SwingContext` (CLUTCH pressure power bonus, POWERHOUSE power base, CANNON_ARM 0.85 window, spin-read penalty with SWITCH immunity); FieldingModule ability params (IMMOVABLE guaranteed stationary catch — zero rng draws, BUTTERFINGERS fumble-to-feet with the play-scoped `fumbledFlight` never-caught guard, LONG_REACH 1.4× radius when stationary (< `STATIONARY_SPEED_EPS`), POWERHOUSE fatigue floor, QUICK_DRAW half throw delay); MatchRoom threads real per-character contexts into every module call; WALL blocker (whale, `WALL_BLOCKER_HALF_HEIGHT 0.9`/`WALL_BLOCKER_RADIUS 0.4` capsule) armed for the contactMade window, stop-dead velocity zeroing, never classified `caught`. POWERHOUSE's pitch-side fatigue immunity is inert (no pitch fatigue formula exists — §6.2). **Previous milestone 8** (positioning + substitutions + stamina ledger, §9.8) COMPLETE, MERGED and TAGGED `m8-positioning` (main at 7be99ec, 291/291 at merge). M8 delivered: per-side PositioningModule (custom positions + bench + per-innings sub counter, `LEGAL_ZONE` + `BATTING_SQUARE_KEEPOUT` validation, layouts persist for the side across the whole game, fresh on rematch); `reposition {id,x,z}` / `substitute {outId,inId}` (fielding side) and `setBatter {id}` (batting side), all INITIAL_POSITIONING|PRE_PLAY only, paused-first, `wrongRole` exact; room-layer pitcher guard (`'the pitcher moves via setPitcher'`) + benched setPitcher nominee rejected; persistent cross-play stamina ledger (absorb at endPlay top → bench regen +1 capped at stat → rebuildFielding re-seeds from the ledger; rematch re-seeds at stat); layout-driven `rebuildFielding` with the pitcher pinned to `PITCHING_SPOT` and the default re-derived from the ON-FIELD set (benched best-arm cannot bowl; subbing the bowler out re-derives); `fieldSlotsOverride` test-only room option (real benches from 5-a-side squads); schema `benchA/B`, `subsUsedA/B`, `queueIds`; client raycast repositioning (click own non-pitcher capsule → click ground; Escape clears) + panel modes (fielding: select rows / bench substitute rows / exact empty text `bench — awaiting roster growth` + subs-used heading; batting: queue rows set the next batter) over a single-source `SelectionStore` (store.set mirrors the 3D highlight — no second writer). Batting-side substitution deferred (USER-APPROVED scope). (Tags: M1 `m1-scaffold`, M2 `m2-physics`, M3 `m3-pitch-hit`, M4 `m4-fielding-running`, M5 `m5-rules`, M6 `m6-sync`, M7 `m7-draft`, M8 `m8-positioning`, M9 `m9-abilities`, M10 `m10-ui-polish` — ALL pushed to origin.)
- **Modules implemented:** `/shared` (types incl. StatBlock/AbilityId/Character/PitchInput/SwingInput/RunDecisionInput/FielderSetup + **TeamSide/PlayResolution/SettlementFact; PlayOutcome runOut/safe now carry runnerId**; deep-frozen CONST; formulas.ts — ALL §5 formulas; characters.ts — §3 roster; rng.ts — mulberry32 `createRng`); `/server` (PhysicsModule (M2+M4 surface unchanged); PitchModule; HitModule (+optional `pressure` param → timingFactor × pressureMult); FieldingModule (+`pressure?: () => boolean` dep → pCatch × pressureMult); **RunningModule — MULTI-runner**: shared stop/go, forced-on cascades at occupied posts, per-runner `exposures()`, `markOut(id)`, `settlePlay()` → SettlementFact[] with collision-free parking; **RulesModule — pure state machine**: phase walk LOBBY→…→GAME_OVER→rematch, A/B innings pairs ×INNINGS_COUNT, batting-queue rotation (home re-queues, out leaves), half-rounder scoring, sudden-death tiebreak pairs, `pressure()`; **MatchRoom — real phase machine**: rules-mirror schema (scoreHalvesA/B, inningsIndex, outs, battingSide, currentBatterId/PitcherId, tiebreak, winner), `runners` MapSchema, message validation matrix (pitch/swing/runDecision PLAY-only; readyForPlay PRE_PLAY; confirmPositioning INITIAL_POSITIONING; rematch GAME_OVER) with structured `rejected` broadcast + `lastRejection` mirror, per-runner exposure-scoped run-out (M4 guard discipline generalised to exposure SETS), JSON `PlayResolution` in `lastOutcome` + `playOutcome` broadcast); `/client` (NetModule — full M5 state view + confirmPositioning/readyForPlay/rematch senders + playOutcome/rejected handlers; InputModule — **Enter confirm/ready, N rematch** (+ M3/M4 keys); RenderModule — per-id reconciled **runners map** (fielder pattern); main.ts — `phase | A x½ – B y½ | innings i | outs o | batter` status line + PlayResolution summary). **M6 deltas:** `/shared` +`GAME.RECONNECT_GRACE_S`; `/server` (M5 modules untouched; **MatchRoom** — sessionA/B seats by join order + `sideOf`, role gating on pitch/swing/runDecision, both-side confirm/ready sets, `paused` gate on every handler + tick freeze (sim time holds, so play timers survive a pause), `onLeave` consented → `opponentLeft`+dispose / unconsented → pause + `allowReconnection(grace)`, `shuttingDown` re-entrancy guard, `MatchRoomOptions {code?, reconnectGraceS?}` runtime-validated; **MatchState** +roomCode/sessionA/B/connectedA/B/paused; app.config `filterBy(['code'])`); `/client` (**NetModule** `connect({mode:'create'|'join'})` with crypto-random 4-letter codes, `mySide()`/`myRole()`, opponentLeft handler; **InputModule** detachable single live keydown handler, keys act only for the local role; **main.ts** lobby overlay — create shows the code from SYNCED state (b5608ca), join by code, waiting/paused/opponent-left states). **M7 deltas:** `/shared` (+`DraftPickInput`/`SetPitcherInput` message types); `/server` (**DraftModule** — NEW pure alternating draft: `createDraftModule(pool, picks)` with `view()`/`pick(side,id)`/`squads()`, `picksEach(poolSize)`; **RulesModule** +`completeDraft(squads)` — replaces the placeholder mirror squads, rebuilds the batting queue via re-run `startInnings(inningsIndex)`, DRAFT→INITIAL_POSITIONING; **MatchRoom** — `draft`/`squads`/`pitcherId`/`builtFieldingSide` fields, `handleDraftPick`/`handleSetPitcher`, `defaultPitcherId` (highest pitch, tie→earlier pick), `rebuildFielding()` (nominee on slot 0, rest in pick order; no-op pre-draft; pitcher re-derived on side change; called on draft completion, every endPlay, rematch, setPitcher), `syncDraft()`; onCreate starts a placeholder `createFieldingModule([], …)` so pre-draft syncs are safe; **MatchState** +draftTurn/draftRemaining/squadAIds/squadBIds); `/client` (**DraftScreen** — NEW `#draft` scorer's-sheet overlay: one clickable row per roster character (name, 9-stat line, ability tag), draft mode = pick grid with [A]/[B] badges + your-turn gating, pitcher mode = fielding side's own squad with the [bowling] mark, container-scoped click delegation, optimistic double-click disable; **NetModule** +draft state view + `sendDraftPick`/`sendSetPitcher`; **main.ts** DRAFT/bowler status-line segments). **M8 deltas:** `/shared` (+`RepositionInput`/`SubstituteInput`/`SetBatterInput` message types; CONST.FIELD +`LEGAL_ZONE`/`BATTING_SQUARE_KEEPOUT`/`PITCHING_SPOT`; `FielderSetup` +optional `stamina` seed); `/server` (**PositioningModule** — NEW pure per-side state: `view()`/`reposition(id,x,z)`/`substitute(outId,inId)`/`resetSubs()`, seeds `FIELDING_POSITIONS` in pick order so the default layout equals the old M7 slot map; **RulesModule** +`setNextBatter(id)` (displaced batter → queue FRONT) + `queue` in the view; **FieldingModule** honours the `stamina` seed incl. through `reset()`; **MatchRoom** — `positioning` per-side modules + `staminaById` ledger + `fieldSlots`, `handleReposition`/`handleSubstitute`/`handleSetBatter`, on-field-derived `defaultPitcherFromIds`, layout-driven `rebuildFielding` (pitcher pinned to PITCHING_SPOT), endPlay ledger absorb→regen, per-innings `resetSubs`, rematch re-seed, `fieldSlotsOverride` option; **MatchState** +benchA/B, subsUsedA/B, queueIds); `/client` (**PositioningControls** — NEW canvas raycast select/move + Escape clear, detachable like InputModule; **DraftScreen** fielding/batting panel modes (bench + queue rows, `[selected]`/`[batting]` badges, empty-bench note); **RenderModule** `pickId` raycast + `setSelected` scale highlight; **NetModule** +reposition/substitute/setBatter senders + M8 state view; **main.ts** single-source `SelectionStore` + subs-used status segment). **M9 deltas:** `/shared` (**abilities.ts** — NEW registry: `FieldingAbilityParams`/`PitchAbilityMods`/`HitAbilityMods` + `fieldingAbilityParams(c)`/`pitchAbilityMods(c)`/`hitAbilityMods(c)` pure derivations from `Character.ability`; **constants.ts** +`CONST.ABILITY` (CLUTCH_POWER_BONUS 3, CURVE_SPIN_MULT 1.6, CURVE_ONSET_FRACTION 0.6, LONG_REACH_RADIUS_MULT 1.4, STATIONARY_SPEED_EPS 0.1, QUICK_DRAW_DELAY_MULT 0.5, CANNON_PITCH_BONUS 3, POWERHOUSE_FATIGUE_FLOOR 2, SPIN_READ_W 0.25, WALL_BLOCKER_HALF_HEIGHT 0.9, WALL_BLOCKER_RADIUS 0.4); **formulas.ts** +`spinReadPenalty(spinStat, spinInput)`; **types.ts** `PitchParams` +optional `curveOnsetS`); `/server` (**PitchModule** applies `PitchAbilityMods` (CANNON pitch bonus, CURVEBALL spin mult + `curveOnsetS` emission); **PhysicsModule** Magnus onset gate — no lateral force before `curveOnsetS` sim time — + WALL blocker stop-dead (`setBlocker` capsule now zeroes ball velocity on contact instead of rebounding); **HitModule** +`SwingContext` (`powerBonus`/`powerBase`/`timingWindowMult`/spin-read fields, `NEUTRAL_SWING_CONTEXT` default) — CLUTCH adds power under `pressure()`, CANNON_ARM shrinks the window ×0.85, spin-read penalty multiplies the window unless the batter has SWITCH; **FieldingModule** takes per-fielder `FieldingAbilityParams` — IMMOVABLE stationary catch is guaranteed (no rng draw), BUTTERFINGERS fumbles to the fielder's feet with the play-scoped `fumbledFlight` guard (that flight can never later be `caught`), LONG_REACH 1.4× catch radius while stationary, POWERHOUSE floors `fatigueMult` at 1 above stamina 2, QUICK_DRAW halves `THROW_RELEASE_DELAY_S`; **MatchRoom** threads real ability contexts (pitcher mods into pitch, batter `SwingContext` incl. the live pitcher's spin stat into swing, per-fielder params into rebuildFielding) and arms/disarms the whale's WALL blocker over the contactMade window). Client untouched (§9.9 changes no UI surface). **M10 deltas (client-only; `/server` + `/shared` untouched):** `/client` (**NetModule** — reconnect surface: module-level `reconnectionToken` persisted on every join/reconnect, `wrapRoom` shared by `connect` and `tryReconnect` so the two paths cannot drift, `onUnexpectedDisconnect` fires AT MOST ONCE per Net (one drop can surface as onLeave, onError, or both — single `disconnectFired` latch) and is suppressed by `markLeaving()`/opponentLeft, `tryReconnect()` = ONE attempt with the stored token resolving a FRESH Net or null; **InputModule** — gameplay keys (A/S/D/P/Space/R/T) inert while `state.paused` (Enter/N stay phase-checked + server-validated); **RenderModule** — self-animating views: each view runs its own rAF loop lerping towards the last-known schema target with the TIME-based factor `1 − 0.001^dt` (framerate- and patch-rate-independent — root fix for the M8 sparse-patch midway-capsule quirk), `runners.markOut(id)` = red tint (0xc0392b) + 90° topple + frozen position retained 1.5 s past schema delete, with revival guards (expired retention or a genuinely-absent-then-returning id revives; a straggler pre-delete patch does not) and a dev-aid console.warn on the no-mesh race; **UIModule** — NEW: scorer's board (phase + `you are A/B · batting|fielding` head, big `A x½ – B y½` score, gold `[TIEBREAK]`/`[PRESSURE]` badges, dotted-leader rows innings/outs/batting/bowling(+subs used when fielding); hidden in LOBBY/DRAFT), 6-entry newest-first event feed, contextual key legend (full 7-key vocabulary greyed with MY live subset lit: positioning→Enter+fielding mouse hint, PLAY batting→Space/R/T, PLAY fielding→A/S/D+P, GAME_OVER→N, DRAFT→mouse note; `paused — waiting for reconnect` override; `setNotice` standing override for the dead-room reconnect window), GAME_OVER result overlay (score, `WINNER: A|B`, `after n innings[ · tiebreak]`, REMATCH/LEAVE `.card-button`s), `describeCause`/`describeResolution` moved VERBATIM from main.ts; **main.ts** — `runMatch` rebuilt around one idempotent `teardown()` (detach input + positioning, clear selection, `ui.reset()`, clear `active`; `torn` also gates every straggler callback), reconnect flow (feed `connection lost — reconnecting…` + notice → `tryReconnect()` → success: teardown + `runMatch(fresh)` + `reconnected` / failure: lobby `connection lost`), result buttons wired ONCE at module level against the live match (rematch → `sendRematch()`, the same either-player message as the N key), feed edge events (opponent joined / rematch started / game paused / game resumed), rejection map (`wrongRole`→`not your role`, `paused`→`game is paused`, else prose verbatim); **index.html** — `#status` debug line REMOVED, `#hud` (board/feed/legend/result) added, lobby restyled (letterspaced title plate, two ruled-off paths, share-cue + 2.6em gold code); **DraftScreen** — batting panel's disabled stats row replaced by the `now batting: Name` info row + `parked: Name @ post n` rows from `state.runners`, empty-bench note full-width).
- **Test status:** **380/380 passing** (137 shared: 11 types + 15 abilities + 16 formulas + 15 characters + 76 constants + 4 rng; 243 server: 6 draft + 12 pitch + 6 positioning + 18 hit + 24 running + 26 rules + 53 fielding + 20 autoplay + 35 physics + 43 room); `npm run check` green ×2 on `worktree-autoplay-redesign` — typecheck ×3 clean, ESLint clean, 16 files / 380 passed, exit 0 both runs (**test phases 839 s / 707 s — the suite now runs ~11–14 min**, dominated by the migrated MatchRoom suite watching real-time auto-play beats incl. two full-game marquees; §6.4 runtime note; T3 also ran the full check green ×2). The client stays deliberately test-less per the M1 precedent — verified by browser acceptance instead. **Auto-play redesign acceptance (T7, 2026-07-05):** scripted — two colyseus.js clients vs this worktree's real server (`docs/superpowers/acceptance/autoplay-acceptance.mjs`, log `autoplay-acceptance.txt`, 50 OK / 0 FAIL, exit 0; harness takes the ws URL as an argument — this run used port 2568 because a stale unkillable pair from another session squats 5173/2567, §6.3 note): full 24-play game to GAME_OVER (winner B 4½–1½) with ZERO play messages sent by either client and zero rejections received; per-play roll-sequence collector green on all 24 plays (pitch first; every swing after its pitch; every re-pitch after a FAILED swing; run/catch only post-contact; 146 rolls {pitch 41, swing 41, run 27, catch 37}); COUNTER-CLOCKWISE from the runner schema (first runner's opening leg into negative x — first ≥3 m sample (−2.86, 1.04), min-x −10.97; first parked arrival post 2 at x = −9); tombstones exact from the ex-owner roles; pause froze the dice (zero rolls across 1.2 s, ball frozen) and reconnect resumed them. Browser — Playwright 1.61.1/Chromium, two pages (`autoplay-browser-acceptance.mjs`, log `autoplay-browser-acceptance.txt`, exit 0, screenshots `autoplay-0*.png`): restyled lobby/draft clicked through the real UI; legend free of retired play keys; orbit drag changed the render + Home reset; reposition FROM THE ORBITED CAMERA accepted ([selected] badge off the schema patch, zero rejections, selection-ring pixels 0→13 at the click target); hands-off play watched — crisp roll banner (animation-frozen clone of the real first banner, capture-lag workaround), batter at the square pre-contact, runner mid-circuit screen-right, resolution with zero client play messages, ≤2 banners stacked, zero page errors; close-up second play caught Kian mid wind-up with the ball just released and the batter's bat visible in profile (autoplay-05-windup-a). **M10 §9.10 acceptance (Task 4):** browser — TWO Chromium pages in SEPARATE Playwright 1.61.1 contexts vs a real `npm run dev` server (`docs/superpowers/acceptance/m10-browser-acceptance.mjs`, log `m10-acceptance.txt`, assertion-accumulating, exit 0, 109 s; screenshots `m10-01..04-*.png`): restyled lobby create (title plate, `share this code with your opponent` cue, gold code QUNN read from the DOM) + join by code; full 10-pick table-order draft clicked through the sheet; a COMPLETE 20-play game driven end-to-end by REAL KEYDOWNS (Enter confirm/ready on both, fielding page A/S/D+P, batting page Space) across all 4 innings to GAME_OVER — EVERY play asserting the feed's resolution line and that BOTH pages' boards equal the score accumulated from the feed's own `+N½` deltas (final A 5½ – B 1½, 7 safes/12 caught/1 runOut); first play asserted the legend lighting EXACTLY `[Space] swing/[R] run/[T] stop` on the batting page and `[A/S/D] spin/[P] pitch` on the fielding page (m10-02); result overlay (score = accumulated total, `WINNER: A`, `after 4 innings`, `[N] rematch` lit; m10-03) then REMATCH CLICKED → both pages score-zeroed, overlay gone, feed `rematch started`, fresh game resting in INITIAL_POSITIONING (m10-04); **reconnect drill** — page 2's game WebSocket force-closed via an init-script hook (unconsented drop; 800 ms CDP latency on page 2's HTTP widened the pause window): page 1 feed `game paused — opponent disconnected` AND the transient `paused — waiting for reconnect` legend notice caught mid-window, page 2 `connection lost — reconnecting…` → `reconnected` (the shipped ONE-ATTEMPT auto-reconnect, ~1.7 s), page 1 `game resumed`, then Enter on both advanced BOTH boards to PRE_PLAY — the fresh room's state stream and re-wired input proven live on both sides; out-runner topple VERIFIED by screenshot pixel-sampling (536 red px vs 0 baseline within the 1.5 s retention after `Darcy run out at post 2`; a toppled red capsule is also visible in m10-03); `context.setOffline(true)` probed and DOCUMENTED as not affecting the established WebSocket in this Chromium (known CDP network-emulation limitation — hence the raw-socket drill); zero page errors and zero console errors on both pages across the whole run; `RunnersView.markOut` console.warn counted at 9 per page over 13 outs (bounded — one per race-lost out, not a spam storm; §6.4 note). Harness note: a missed swing is BY DESIGN not a play resolution (the ball silently respawns and PLAY awaits a re-pitch — MatchRoom M3 behaviour), so the harness retries the pitch/swing pair when no resolution arrives; 2 of 20 plays needed one retry. **M9 §9.9 acceptance (Task 6):** scripted — TWO colyseus.js clients vs a real `npm run dev` server (`docs/superpowers/acceptance/m9-acceptance.mjs`, log `m9-acceptance.txt`, assertion-accumulating with non-zero exit on any failure; exit 0, 48 s; browser run deliberately omitted — §9.9 changes no UI surface). Custom draft put kian+whale+jonty+joe on side B (kian still the default bowler with the custom five); whale parked at (12,28), jonty at (−6,9). Demo 1 CURVEBALL late onset: two sampled no-swing pitch flights (spinInput 1) merged into a (z,x) profile bracketing the onset point — |x| deviation 0.0003 m over the first 60% of flight vs 0.0367 m over the last 40% (late window dominates > 2×), total |x(plane)| 0.0370 m > 0.015. Demo 2 WALL: a genuinely fast flat drive (> 5 m/s post-contact) down the (12,28) corridor collapsed to < 0.5 m/s within 1.8 m of the whale at ground level (d=0.11 m, y=0.02) and resolved `safe` — never `caught`. Demo 3 IMMOVABLE: flat drive at jonty on a live seed-only room → `{"kind":"caught","by":"jonty"}` (guaranteed catch, zero rng draws). Demo 4 BUTTERFINGERS: bounded numeric-seed search pre-screened via a mulberry32 replica (candidates logged; seeds tried live: 7, 19 — documented in-log) → live at seed 19: in-flight approach into joe's radius, ball velocity zeroed at his FEET (y=0.04; a gather parks at hands y=1.0), no caught-out (outcome `safe`). ONE substituted demo, honestly logged in the acceptance log: demo 5 (spin-read/CANNON live swing-window comparison) — live sub-tick timing did not land the 49.5 ms discrimination band, so it fell back to the unit-test window maths (HitModule.test.ts spin-read laurie-vs-darcy 0.1705 s vs 0.22 s at spin 9 input 1, and the CANNON_ARM 0.85 window test). **M8 §9.8 acceptance (Task 5):** scripted — TWO colyseus.js clients vs a real `npm run dev` server (`docs/superpowers/acceptance/m8-acceptance.mjs`, log `m8-acceptance.txt`, exit 0, 42 s). ROOM 1 (9 slots): full table-order draft; every reposition rejection with its EXACT reason (batting side `wrongRole`; pitcher `the pitcher moves via setPitcher`; out-of-zone (999,20) and keep-out (1,1) `illegal spot or not an on-field fielder`; during PLAY `only allowed in INITIAL_POSITIONING or PRE_PLAY (phase PLAY)`); josh repositioned to (5,20) — schema fielder moved and SURVIVED confirm→ready into PLAY; 11 plays driven across two innings switches — A's five with default joel at the first, and at innings 3 B's five returned with kian RE-derived AND josh still at the custom (5,20). ROOM 2 (`fieldSlotsOverride: 3`): benchB = robbie,ricy; substitute josh→ricy synced bench+count (ricy inherited josh's spot); setPitcher darcy pinned to the pitching spot, benched josh nominee → `benched — substitute them on before nominating`, subbing the bowler out (darcy→josh) re-derived kian; stamina ledger — ricy chased a loft and came back next play at 7.134/8 (drain persisted; kian also drained to 5.723/6), then after sitting a play out returned at exactly min(8, 7.134+1) = 8.000; setBatter joe flipped `currentBatterId` with displaced carl at the queue head (restore via a second setBatter also proven), fielding-side attempt `wrongRole`; subsUsed incremented freely 1→4 (casual cap Infinity never hit). Browser — Playwright 1.61.1/Chromium (`m8-browser-acceptance.mjs`, screenshots `m8-01/02/03-*.png`): two pages clicked the ENTIRE draft through the real UI; the fielding page selected josh via its PANEL row (shared selection store) then clicked a computed GROUND pixel (Node-side three.js replica of the client camera projecting (−8,24)), verified by screenshot pixel-sampling — 0 capsule-blue px at the target before, 391 after, 0 left at the old slot — plus the `[selected]` badge; panel showed `bench — awaiting roster growth` + `subs used: 0`; the batting page clicked queue row Joe → status announced `batter: Joe` and displaced carl re-rendered at the queue head. (Canvas clicking needed a 7-click convergence burst because of the client's per-patch lerp — §6.4 note.) **M7 §9.7 acceptance (Task 5):** scripted — TWO colyseus.js clients vs a real `npm run dev` server (`docs/superpowers/acceptance/m7-acceptance.mjs`, log `m7-acceptance.txt`, 38/38 checks, exit 0): create/join by code MSVN → room rests in DRAFT; out-of-turn pick → `rejected {reason:'wrongRole'}` to the offender only; full table-order draft with one taken-id re-pick → prose rejection `unknown or already-picked character`, turn preserved; exact squads synced (A carl,laurie,joel,jonty,joe / B kian,josh,darcy,robbie,ricy, whale the sole `draftRemaining`) → INITIAL_POSITIONING; default bowler kian on FIELDING_POSITIONS[0], batting-side setPitcher → `wrongRole`, fielding-side nomination ricy applied (pitcher flips + takes slot 0) then restored; full game to GAME_OVER (23 plays, 3 innings switches, caught 10/runOut 2/safe 11, winner A 5½–0½) asserting at EVERY switch that the fielding five are exactly the new side's drafted squad with its DEFAULT pitcher (A fields → joel; B again → kian re-derived), and that the whale never batted. Browser — Playwright/Chromium (`m7-browser-acceptance.mjs`, screenshots `m7-01/02/03-*.png`): two pages clicked through the ENTIRE draft via the real card UI (each page only on its own turn, read from its own DOM; clicked sequence = the table order), mid-draft badges verified, page A's sheet hides on completion, page B's becomes the `nominate bowler` strip ([bowling] on kian, only B's squad rows non-hidden), clicking Ricy moves the [bowling] mark and the status line reads `bowler: Ricy`. **M6 §9.6 acceptance (Task 5):** scripted — TWO colyseus.js clients vs a real `npm run dev` server (`docs/superpowers/acceptance/m6-acceptance.mjs`, log `m6-acceptance.txt`): A creates with code MSIX / B joins by code into the SAME room, full game to GAME_OVER with every message from its role-correct client only (95 plays, 3 innings switches, caught 30 / runOut 7 / safe 58, winner A 51½–0½), one deliberate wrong-role pitch → `rejected {reason:'wrongRole'}`, consented quit → `opponentLeft {side:'B'}` + room disposal; second room (code RCON): raw-socket drop mid-flight → `paused=true` with ball frozen at identical x/z across 1.2 s, swing while paused → `rejected {reason:'paused'}`, `client.reconnect(token)` → unpaused, ball resumed (z 5.7426 → 5.3041). Browser — Playwright/Chromium on THIS Windows machine (`m6-browser-acceptance.mjs`, screenshots `m6-01/02/03-*.png`): page 1 Create shows code NQPK in the lobby overlay (read from the DOM), page 2 joins with it, BOTH pages reach INITIAL_POSITIONING. **M5 §9.5 acceptance (Task 7):** scripted colyseus.js WS client vs a real `npm run dev` server, two complete games — (1) mode `game` seed 42: 76 plays, 3 innings switches, tally caught 31 / runOut 5 / safe 40, half-rounders banked (final A 29½ – B 1½), one deliberate out-of-phase pitch → structured `rejected`, GAME_OVER winner A, rematch → INITIAL_POSITIONING zeroed; (2) mode `tiebreak` seed 7: all-scoreless plays force 0–0 after both innings pairs → sudden-death TIEBREAK entered, A banks 1½ on its tiebreak play, B held to 0 → winner A after the pair. Browser acceptance (Playwright/Chromium vs Vite 5173): real keydown walk Enter→Enter→P→Space produced `last: Carl safe at post 3 · +1½` on the status line; screenshots show 9 fielder capsules, runner capsule, ball, posts/squares. Evidence committed: `docs/superpowers/acceptance/m5-acceptance.mjs`, `m5-acceptance-{game,tiebreak}.txt`, `m5-0{1,2,3}-*.png`. **Deviation:** the plan's "at least one rounder" is UNREACHABLE live (circuit ~47.4 m vs PLAY_TIMEOUT_S 6 — TUNING.md M5 note); the rounder path is covered by RulesModule unit tests.
- **Open worktrees/branches:** none — the AUTO-PLAY REDESIGN is MERGED and TAGGED: main fast-forwarded 46c8224 → 980a120, tagged `autoplay-redesign`, main + tag pushed to origin 2026-07-05. Final whole-branch review verdict "Ready to merge? Yes" — beat lifecycle/reset paths/pause-reconnect/mirror completeness/roll-listener teardown all traced clean; the dice-replace-inputs-never-resolution safety property verified by construction; its one comment-level minor (wind-up timing comment described a wrong mechanism) fixed pre-merge at 980a120; deferred minors logged (pitch roll verdict vs visible curve mismatch — presentation only; CLASSIC_LOOKAT +x framing bias — tuning; double-click camera reset not drag-suppressed). The checkRunOut exposure-guard follow-up (§6.4) remains the named next engineering item — the reviewer recommends the pure-function extraction variant. Worktree + branch removed. Previous: the visual overhaul is MERGED and TAGGED: main fast-forwarded 369c093 → ba56fec, tagged `visual-overhaul`, main + tag pushed to origin 2026-07-05; final whole-branch review verdict "Ready to merge? Yes" (zero-gameplay-impact guarantee verified at diff level; probed seams clean: pose contract on all rigs + DEFAULT, kit rebuild × holder/dying state, reconnect view reuse, dispose/pick-map hygiene, chalk canvas transform; deferred minors: dying-runner kit-rebuild tint edge (unreachable today — consider a reapplyStatus helper), tri-count comment drift, holder arm un-cock snap). Worktree + branch removed. `claude/worktree-completion-merge-bvswsy` remains on origin pointing at the M5 merge commit (harmless; delete at leisure). **Tags:** m1–m10 + `visual-overhaul` + `autoplay-redesign` all pushed to origin.

### 6.2 Decisions Record (append-only)

| Date | Decision | Reason |
|------|----------|--------|
| 2026-07-03 | npm workspaces (not pnpm) | Matches `npm run …` commands mandated in §3; stated in README. |
| 2026-07-03 | Field geometry (post/square coordinates in CONST.FIELD) is a placeholder school-rounders layout | Spec gives no pitch dimensions; structural tests only pin 4 posts + positive dimensions; tune in playtest. |
| 2026-07-03 | `BENCH_STAMINA_REGEN = 1` per play | Spec §4 names the constant but gives no value; revisit when fatigue lands (Milestone 4). |
| 2026-07-03 | typescript-eslint v8 (plan said v7) | v7 requires ESLint 8; repo uses ESLint 9. |
| 2026-07-03 | Server tsconfig keeps base Bundler/ESNext resolution (plan said NodeNext override) | NodeNext broke extensionless source-level `@carlquest/shared` imports and `@colyseus/tools` CJS interop under tsx. |
| 2026-07-03 | `server/src/app.config.ts` exports a typed plain `ConfigOptions` object rather than calling `@colyseus/tools` `config()` | The default-import `config()` call crashes under real Node ESM (CJS interop); invariant kept: one config shared by index.ts and @colyseus/testing. |
| 2026-07-03 | `server/vitest.config.ts` pins `pool: 'threads'` | Vitest 2.1.9 forks pool crashes on Windows IPC. |
| 2026-07-03 | M2 new tunables: `BALL_RELEASE_HEIGHT = 1.0`, `GROUND_THICKNESS = 0.1`, `POST_SENSOR_RADIUS = 0.5` | Spec §6 silent on these; needed for spawn default, cuboid ground, run-out sensors. |
| 2026-07-03 | Whale `WALL` blocker collider deferred to Milestone 4 | Spec §6 says "active during fielding"; FieldingModule doesn't exist until M4. |
| 2026-07-03 | Ball collider uses `CoefficientCombineRule.Max` for restitution | Rapier default Average blends ball 0.4 with unset ground 0 → effective 0.2; Max makes the spec's 0.4 the effective coefficient. |
| 2026-07-03 | `@dimforge/rapier3d-compat` pinned at ^0.14.0 | First Rapier usage; version recorded for determinism (same binary ⇒ same trajectories). |
| 2026-07-03 | `placeBall` resets the step accumulator (pitch re-anchors substep phase) | Makes pitch trajectories independent of message arrival within a frame. The accumulator is WORLD time — MUST be revisited in M4 when fielder bodies join the world (final-review finding). |
| 2026-07-03 | Hit launch direction from the batter's aim vector, elevation clamped −10°..60° | Spec §1 names launch angle but §5 gives no formula; USER-APPROVED choice (aim-based) over timing-derived alternatives. |
| 2026-07-03 | M3 new tunables: HIT_ELEVATION_MIN/MAX_DEG −10/60, PITCH_ELEVATION_MAX_DEG 20, PLAY_TIMEOUT_S 6, BALL_REST_SPEED 0.1, BALL_REST_TIME_S 1, SIM_MAX_CATCHUP 0.25 | Spec silent; needed for aim clamps, demo play-end and the tick clamp. |
| 2026-07-03 | Client `swing.timing` field accepted but IGNORED in M3; server sim-time (batting-plane crossing) is authoritative | Server-authoritative principle; latency compensation revisited in M6 networking. |
| 2026-07-03 | Pitch/hit spin input = scalar in [−1,1] mapped to vertical-axis sidespin | §7 gives `spinInput` without semantics; sidespin is what Magnus turns into visible curve. |
| 2026-07-03 | Demo cast fixed: pitcher Kian, batter Carl | M3 single-player loop needs stats before the draft exists (M7). |
| 2026-07-03 | Degenerate aim vectors (zero, non-finite, purely vertical) fall back to sane defaults instead of throwing | Player input must never crash the room or inject NaN into physics (review finding, fixed in both modules). |
| 2026-07-03 | M4 Tasks 1–4 implemented in an external Claude session, delivered as `carlquest3m4.bundle`, verified locally (`npm run check` 175/175) before fast-forward merge | User-directed workflow for this batch. |
| 2026-07-03 | `RunDecisionInput` message shape added to §7 contracts | USER-APPROVED deviation (per bundle commit 5823b2c); spec §7 lacked a run stop/go message. |
| 2026-07-03 | Fielders are kinematic (module-computed positions), NOT Rapier bodies; ball remains the world's only dynamic body | Closes the M2 `placeBall` accumulator-reset caveat by design (no other body loses world time). WALL blocker capsule API exists but activation lands in M9. |
| 2026-07-03 | M4 new tunables: APPROACH_W 0.35, APPROACH_REF_SPEED 30, THROW_RELEASE_DELAY_S 0.5, SPRINT_STAMINA_COST_PER_S 0.15, THROW_STAMINA_COST 0.5, CATCH_HEIGHT_MAX 2.5; FIELDING_POSITIONS 9-slot placeholder layout (slot 0 = bowling square) | Spec silent; needed for catch rolls, throws, fatigue. Real positioning UI lands M8. |
| 2026-07-03 | Deterministic catch rolls via seeded mulberry32 `createRng` in `/shared/rng.ts` | Server-authoritative determinism; one pCatch roll per catch-radius entry (entry-latched). |
| 2026-07-03 | Fielding ability hooks left as neutral defaults | Abilities (QUICK_DRAW etc.) wire in at M9 per plan. |
| 2026-07-03 | Run-out detection is event-accurate: `PhysicsModule.wasBallAtPost(n)` latches Rapier `EventQueue` sensor-intersection events per substep (post sensors gain `ActiveEvents.COLLISION_EVENTS`), reset on spawn/pitch/hit; `MatchRoom.checkRunOut` uses it (dropped the `sweptPost` proximity heuristic) | Task-5 review Important 1/2: fixes the §6.4 once-per-tick-poll fly-through gap the documented way (events, not a widened capture volume), removing the detection-side test flake. Residual retry is ONLY on the catch test — absorbs which-fielder-is-nearest jitter from sub-tick swing-message landing (`waitForNextSimulationTick` is a fixed timer), never detection; run-out is single-attempt (10×/10 stable). |
| 2026-07-03 | Run-out crossings are scoped to the runner's exposure window: MatchRoom tracks `exposedPost()` transitions and calls new `PhysicsModule.clearPostCrossings()` on any change (window opens clean at the hit); `checkRunOut` = `wasBallAtPost` (during-exposure crossing) OR `isBallAtPost` (ball already resting inside the sensor — entry events don't re-fire) OR holder-at-post | Task-5 fix round 2: a segment-lifetime latch was a reachable false run-out (hit flies through post 2 early, runner later sets off for post 2 → stale crossing ran them out). Rule is "ball at the exposed post WHILE exposed". Regression-tested at both layers. All test retries now removed — catch test is single-attempt asserting `kind === 'caught'` (who catches is nearest-fielder jitter, deliberately unpinned). 10×/10 stable. |
| 2026-07-03 | Demo fielding side (M4): the first 9 roster entries *excluding the demo batter Carl*, in table order, mapped onto `FIELDING_POSITIONS` in slot order → Kian lands on slot 0 (the bowler), Laurie backstop, then posts and deep field | The M3 demo cast fixes only pitcher/batter; the fielding nine needs a deterministic source before the M7 draft. Table-order-minus-batter is the simplest stable choice; positioning is refined in M8. The integration test derives `FIELDING_IDS` the same way so it cannot drift. |
| 2026-07-03 | Play-end while the runner is mid-segment (rest/timeout, not run out) resolves as `safe` at the *previous* post: `runner.atPost ?? (targetPost !== null ? targetPost − 1 : 0)` | Spec §8 has no partial-advance credit; a runner caught between posts at play end is safe where they last stood, not at the post they were heading to. RunningModule guarantees a live non-home runner always has one of atPost/targetPost non-null, so the `: 0` arm is unreachable defensive default (documented inline in `resolveOutcome`). |
| 2026-07-03 | Fielding rng seed source: `MatchRoom.onCreate` uses `options.seed ?? Date.now()` → `createRng(seed)`; `options.rng` (a function) overrides it entirely | `options.seed`/`options.rng` are TEST-ONLY deterministic injection points — a function cannot cross the WS wire, so live clients always fall through to `Date.now()`, the one permissible wall-clock read (it seeds fielding randomness, NOT the deterministic physics step). Unit tests pass `rng: ALWAYS_CATCH/ALWAYS_MISS`; the Task-7 WS acceptance passes a numeric `seed` and searches a small range to exhibit each §9.4 outcome (legitimate demonstration, not the "no magic seed hunting" the module tests avoid). **CORRECTED by the final-review row below: the "TEST-ONLY / live clients always fall through" claim was factually wrong.** |
| 2026-07-03 | Join options are runtime-validated (final-review Important 1): `options.rng` is used only if `typeof === 'function'`, `options.seed` only if a finite number, else `Date.now()` → `createRng` | The previous row's "TEST-ONLY" claim was wrong: `joinOrCreate` forwards the creating client's options to `onCreate`, so `rng`/`seed` are CLIENT-REACHABLE in the M4 demo — the Task-7 acceptance itself passed `seed` over the wire, and a junk `rng` (e.g. `{rng: 1}`) threw `deps.rng is not a function` inside the simulation interval on every catch-radius entry (verified RED in the new integration test; one throw per entry — the entry latch is set before the roll — not a permanent soft-lock, but a real exception storm plus a lost rest-tracking tick each time). A client-supplied numeric `seed` (predictable catch rolls) remains accepted for the demo, same trust class as the logged any-client runDecision, pending M6/M7 role/option gating. |

| 2026-07-03 | M5 rules decisions (USER-APPROVED, brainstorming): half-rounder = 2nd post on own hit (4th post subsumes it at 1 full); multi-runner = shared stop/go full school rules with forced-on at occupied posts; no-balls DEFERRED (spec gives no criteria); tie → sudden-death play pairs | Spec §8 leaves all four open; CLAUDE.md §5 forbids inventing rounders rules, so asked. Design doc: `docs/superpowers/specs/2026-07-03-rules-engine-design.md`. |
| 2026-07-03 | M5 scope notes: score stored in half-rounder integer units; play still ends at first outcome (ball-dead continuation deferred); innings also ends when the batting queue is empty (stranded runners score 0); M5 demo squads = mirror rosters (draft-pool arithmetic contradiction — 9+2 per side from an 11-character pool — deferred to M7); pressureMult wired into timing + pCatch this milestone | Design decisions from the approved M5 spec, logged before planning. |
| 2026-07-04 | Parking-collision micro-rule (RunningModule.settlePlay): a mid-segment survivor parks at the previous post; if that post is already claimed by another surviving runner, the trailing runner is pushed one post further back (repeating until free) | Invented micro-rule the spec cannot answer (flagged in the M5 plan's self-review as the minimal deterministic resolution — two survivors must never share a post). Unit-tested via a forced-on timing construction. |
| 2026-07-04 | Milestone acceptance evidence now lives in `docs/superpowers/acceptance/` (committed), added to the ESLint ignore list alongside `.superpowers/` | `.superpowers/` is git-ignored, and the M3/M4 acceptance harnesses+logs recorded there were lost in the machine/clone handover — evidence referenced from §6.3 must survive the repo, not a session. The dir holds plain Node scripts + logs + PNGs, not project code, hence the lint ignore. |
| 2026-07-04 | M5 browser visual acceptance via Playwright + Chromium (Linux session environment) with REAL keyboard injection | The M4-era headless-Edge blocker (§6.4) does not apply on this machine; keyboard injection is now feasible, so the browser walk exercises the actual InputModule key bindings rather than scripted WS sends. |
| 2026-07-04 | Room code is CLIENT-generated (4 crypto-random uppercase letters in NetModule) and passed as a creation option; the server validates `/^[A-Z]{4}$/` in onCreate and rejects malformed codes | Colyseus `filterBy(['code'])` matches joins against CREATION options — a server-invented code could never match a filtered `join('match', {code})`, so the client must mint it. A rendezvous string, not a secret. |
| 2026-07-04 | Disconnect semantics split by consent: consented leave mid-game → `opponentLeft {side}` broadcast + immediate room disposal (no grace); UNconsented drop → `paused=true`, sim frozen, seat held for `allowReconnection(grace)`, resume on reconnect, opponentLeft+dispose on expiry. **Deviation:** `this.disconnect()` is fire-and-forget (`.catch` logged), NOT awaited, and a `shuttingDown` flag guards onLeave re-entry | Awaiting `disconnect()` inside `onLeave` is a real deadlock — Colyseus disposal waits for every concurrent onLeave to return while `disconnect()`'s promise waits on disposal. And `disconnect()` force-closes the survivor, re-invoking onLeave: unguarded, the consented branch ran twice and broadcast a second `opponentLeft` naming the WRONG side (verified against Colyseus `Room._forciblyCloseClient`; broadcast-spy regression test). |
| 2026-07-04 | `GAME.RECONNECT_GRACE_S = 60`; room creation option `reconnectGraceS` overrides it (runtime-validated finite-positive, junk → constant) | Spec silent on grace length — first guess, TUNING.md candidate. The override exists so the grace-expiry test doesn't wait 60 real seconds; like `seed`/`rng` it is wire-reachable from the creating client (same trust class, same validation discipline, pending M7 option gating). |
| 2026-07-04 | M7 draft sizing (USER-APPROVED): `picksEach = min(SQUAD_SIZE+BENCH_SIZE, floor(poolSize/2))` — 11 characters → 5 picks each, even squads, the leftover (the whale in a table-order draft) stays undrafted; strict A-first alternation, no snake order/timers/undo | Resolves the spec's draft-pool arithmetic contradiction (9+2 per side from an 11-character pool, deferred at M5): a shared exclusive pool cannot supply 11 each. Even squads over a bigger first-pick advantage; design doc `docs/superpowers/specs/2026-07-04-milestone-7-draft.md`. |
| 2026-07-04 | Fielding is PICK-ORDER onto `FIELDING_POSITIONS` with a SEPARATE pitcher nomination: `setPitcher {id}` (INITIAL_POSITIONING\|PRE_PLAY, fielding side, own-squad id — pulled forward from M8, USER-APPROVED); default pitcher = highest pitch stat, tie to the EARLIER pick; the nominee takes slot 0 (the bowling square), the rest follow in pick order; a side change (innings switch/tiebreak/rematch) resets the nomination to the new side's default | Spec §9.7 gives no fielding-assignment rule for drafted squads and M8 (positioning UI) hadn't landed; the bowler is the one slot that must be player-choosable for play to feel like rounders. Tie-to-earlier-pick keeps `defaultPitcherId` deterministic (kian over ricy at pitch 8 for side B). |
| 2026-07-04 | Rematch KEEPS the drafted squads — the draft is NOT re-run; only the innings-1 fielding side is rebuilt with its default pitcher | Spec §4: draft not re-run on rematch. `builtFieldingSide = null` before the rebuild forces the default-pitcher re-derivation. |
| 2026-07-04 | `completeDraft(squads)` re-runs `startInnings(inningsIndex)` to rebuild the batting queue/current batter after the squad replacement (Task 2 deviation, review-approved) | The queue was built from the placeholder mirror squads at `bothConnected()`; replacing squads without rebuilding left ghost batters. inningsIndex is still 0 at draft completion, so the re-run is a pure rebuild, not a phase change. |
| 2026-07-04 | M8 pitcher discipline is ROOM-layer, not module-layer: `handleReposition` rejects the current pitcher with `'the pitcher moves via setPitcher'` (before the module sees it), and `handleSetPitcher` rejects a BENCHED nominee with `'benched — substitute them on before nominating'` | Deviation from the spec's "setter or parameter" wording, locked in the plan header: PositioningModule stays pure/pitcher-ignorant (the room pins the pitcher to `PITCHING_SPOT` in `rebuildFielding`). Without the bench guard, nominating a benched character left `currentPitcherId` pointing at a non-fielder with nobody on the pitching spot (Task-3 deviation, review-approved). |
| 2026-07-04 | Stamina ledger semantics (endPlay, in order): (1) absorb the just-played FieldingModule's drained values into `staminaById`; (2) everyone NOT on the fielding field — both benches AND the whole batting side — regains `BENCH_STAMINA_REGEN` capped at their stat; (3) `rebuildFielding` re-seeds fielder setups from the ledger. Rematch re-seeds the whole ledger at stat | Spec §4 names bench regen but not scope/order. Known quirk (deliberate, no-op today): the regen also touches the ACTIVE batter — invisible because `RunningModule` still fixes runner speed at `startRun` from full-stamina `fatigueMult`; revisit the scope when runner fatigue lands (M9/tuning). |
| 2026-07-04 | `fieldSlotsOverride` room CREATION option (test-only intent, wire-reachable like `seed`; runtime-validated positive integer ≤ FIELDING_POSITIONS.length, junk → full slot count) | The 11-character roster gives 5-a-side squads — below the 9 field slots, so benches are structurally empty live (§6.4). The override lets tests and acceptance exercise REAL benches/substitutions (3 slots → 2 benched per side) without inventing roster entries. Same trust class as `seed`, same pending hardening pass. |
| 2026-07-04 | Batting-side substitution DEFERRED (USER-APPROVED scope): `substitute` is fielding-side only; the batting side manages its innings via `setBatter` (queue re-ordering) only | Spec §4 is silent on swapping a batter out of the innings queue for a benched character; M8 scoped substitutions to the fielding nine where the spec's sub-cap constants clearly apply. |
| 2026-07-04 | Pitcher-change slot-0 overlap accepted as design-inherent: when a nomination displaces the DEFAULT pitcher, the ex-pitcher's persisted layout position is still the bowling square (slot 0), so ex-pitcher and new pitcher stand stacked on `PITCHING_SPOT` until manually repositioned | The layout is per-character and persists by design; auto-relocating the displaced fielder would invent a placement rule the spec doesn't give. Manual reposition is the escape hatch; noted in TUNING.md alongside the slot geometry (seen live in the acceptance: nominating darcy left kian's layout spot = the bowling square). |
| 2026-07-04 | M9 spin-read penalty (USER-APPROVED invented formula, `ABILITY.SPIN_READ_W = 0.25`): the batter's timing window is multiplied by `1 − SPIN_READ_W·s01(pitcherSpin)·|spinInput|`; SWITCH batters are immune | Spec §3 gives SWITCH "reads spin" with no counterpart mechanic for everyone else — without a penalty, immunity is a dead ability. The weight is a first guess (TUNING.md M9 row); design doc `docs/superpowers/specs/2026-07-04-m9-abilities-design.md`. |
| 2026-07-04 | M9 effective stats are UNCAPPED (USER-APPROVED): ability bonuses (CLUTCH_POWER_BONUS +3, CANNON_PITCH_BONUS +3, …) apply on top of the 1–10 stat scale without clamping to 10 | Spec §3 states the bonuses without a cap; clamping would silently nullify them for high-stat characters (e.g. a 8-power CLUTCH batter). Formulas take the raw effective value. |
| 2026-07-04 | `fumbledFlight` play-scoped classification guard (FieldingModule): once BUTTERFINGERS fumbles a flight, every later pickup of THAT flight is classified `gathered`, never `caught`; reset at play `reset()` | Without the guard, a fumbled-then-re-entered ball could still register a pre-bounce `caught` out — a fumble must kill the catch, not defer it. Play-scoped because a new pitch/hit is a new flight. |
| 2026-07-04 | POWERHOUSE's pitch-side fatigue immunity is INERT in M9 (only the fielding-side `fatigueMult` floor is wired) | No pitch fatigue formula exists anywhere in `/shared/formulas.ts` — pitching does not read stamina — so there is nothing to be immune to. The fielding half (fatigueMult forced to 1 at/above `POWERHOUSE_FATIGUE_FLOOR = 2`) is live; revisit if a pitch-fatigue formula ever lands. |
| 2026-07-04 | WALL semantics: "active during fielding" = the `contactMade` window (blocker armed at contact, disarmed at play end); stop-dead = ball velocity zeroed on blocker contact (no rebound); blocker capsule sized `WALL_BLOCKER_HALF_HEIGHT 0.9` / `WALL_BLOCKER_RADIUS 0.4` (spec silent on size — whale-sized first guess, TUNING.md). **Final-review addendum:** the blocker is disarmed while the whale holds the ball and until his release clears the capsule footprint — a self-armed blocker ate his own throws (final-review find; PhysicsModule flight-start exemption: a flight released from INSIDE a blocker's capsule disables that blocker until the ball first exits its footprint, never granted mid-flight, so a final-approach ball is still blocked). | Spec §6 says the whale "blocks" while fielding but gives neither the activation window nor the collision response nor a size. Stop-dead (not a restitution bounce) makes the whale a dead zone rather than a random deflector, and never classifies as `caught` (proven live in the M9 acceptance). |
| 2026-07-04 | M4 blocker rebound test RE-DERIVED to stop-dead semantics (the only M9 test re-derivation) | The M4-era PhysicsModule blocker test asserted a rebound off the blocker capsule; the M9 spec decision is stop-dead (velocity zeroed), so the old expectation was re-derived to assert the ball dies at the blocker — spec-mandated, not a convenience edit. |
| 2026-07-04 | M10 client reconnect is ONE immediate attempt (NetModule `tryReconnect`): on an unexpected drop the client tries `client.reconnect(token)` once; success re-runs `runMatch` against the FRESH Net, failure falls back to the lobby with `connection lost`. **Deviation from the plan's five-method UI surface: `UI.setNotice(text\|null)` added** so the legend can show `reconnecting…` while the room is dead (no state patch can drive it); `reset()` clears it | One attempt covers the realistic recoverable case (a transient socket blip while the network is usable); a sustained outage cannot be retried usefully within the server's grace without a retry/backoff loop the milestone didn't scope — the honest fallback is the lobby. Drilled live in the M10 acceptance (force-closed socket → auto-reconnect → both pages live). |
| 2026-07-04 | Self-animating render views: each RenderModule view runs its own rAF loop converging on the last-known schema target with the time-based factor `1 − CONVERGE_BASE^dt` (`CONVERGE_BASE = 0.001`, a view-local client constant, deliberately NOT in `/shared` CONST — pure presentation, no gameplay meaning) | Root fix for the M8 §6.4 sparse-patch quirk (capsule stuck midway after a single reposition patch): convergence is now independent of framerate AND patch cadence. 0.001 ≈ "remaining distance decays to 0.1% per second", matching the old 50%-per-patch feel under the 20 Hz PLAY stream. |
| 2026-07-04 | M10 batting panel: the old disabled current-batter stats row REPLACED by the `now batting: Name` info row (the plan said "gains a header row"; T3 deviation, review-upheld); result-overlay REMATCH button sends the SAME either-player `rematch` message as the N key (no new server surface) | Keeping both rows would state the same fact in two visual forms on one panel. The M8 evidence asserting the old `[batting]` badge is FROZEN evidence, not a regression suite — not edited, not re-run (same policy as the retired `#status` line the m5–m8 harnesses reference). |
| 2026-07-04 | **CORRECTION (M10 polish, final review):** the plan's client PRESSURE formula, `state.inningsIndex >= CONST.GAME.INNINGS_COUNT * 2 - 1` (the final slot only), was a plan defect — fixed to `state.tiebreak \|\| state.inningsIndex >= CONST.GAME.INNINGS_COUNT * 2 - 2` (the runner-count half of `underPressure` unchanged) | Server `RulesModule.pressure(runnersOnPosts)` is `isFinalInnings() \|\| runnersOnPosts >= 2` with `isFinalInnings() = tiebreak \|\| inningsIndex >= totalSlots - 2` — PRESSURE covers the WHOLE final A/B innings pair, not just its last slot, and `tiebreak` is its own explicit term (a tiebreak keeps `inningsIndex` pinned at the last regular slot, so the old `- 1` form happened to still catch tiebreak plays, masking the off-by-one for the pair's FIRST slot). Do not "restore" the `* 2 - 1` form to match the plan — it under-lights the badge for one whole innings of the final pair. |
| 2026-07-05 | Visual overhaul (post-ship, client-only): capsules replaced by procedural humanoid rigs; the `CharacterModels.VISUALS` table is the SINGLE source of every character's look (height/build/skin/hair/accessory — whale 3.1 m giant, joe 1.3 m scrawny, per the approved design spec's no-pills rule) | Spec §1 gives characters personalities the placeholder capsules couldn't carry; USER-directed overhaul. One table, not scattered per-mesh constants, so a look change is a one-row edit. |
| 2026-07-05 | `KIT_COLOURS`: side A deep navy `0x22345e` / side B maroon `0x7a2231` with shared cream trim; pre-draft/unknown ids render a warm neutral grey kit | unslop pass — a club-cricket pairing that sits with the parchment/gold scorer's-card UI identity rather than a saturated red-vs-blue arcade default; neutral covers the pre-draft window where sides don't exist yet. |
| 2026-07-05 | Stadium budget: static scene 47,740 tris measured via `renderer.info` (crowd instanced at 3,000 cones = 45k of it); whole scene with all 11 rigs 64,612 (~1,535/rig); target ≤ ~75k | Keeps the software-rasteriser headless captures and low-end GPUs comfortable; live play renders ≤ ~6 figures so the real in-game total sits near 57k. The Task-2 comment's original ~33k estimate was wrong — corrected to measured numbers (Task-2 review find). |
| 2026-07-05 | Status-ring colours drive through the ring material's EMISSIVE with the diffuse zeroed (holder gold `0xd9a441`, selected cream `0xf4e9c8`) | Measured during Task 3: under the warm stadium light, Lambert DIFFUSE clips gold and cream to the IDENTICAL rendered colour (both avg rgb 184,153,103) — emissive renders the status colour exactly (selected sampled at exactly 244,233,200; holder at 217,164,65 in the T4 acceptance), keeping the two cues genuinely distinct. |
| 2026-07-05 | Client visuals use a local deterministic LCG (`SceneModule.createLcg`), deliberately NOT `/shared/rng.ts` | Crowd/stand variation is pure cosmetics; importing the server's mulberry32 would entangle presentation churn with the deterministic-sim contract (and invite seed coupling nobody wants). |
| 2026-07-05 | **Auto-play redesign (USER-DIRECTED): spec §7's player play-messages are SUPERSEDED.** `pitch`/`swing`/`runDecision` are no longer accepted from any client; plays resolve as automated server dice beats (AutoPlayModule decisions through the EXISTING resolve paths). The handlers stay registered as tombstones rejecting with exactly `'plays resolve automatically'` (paused-first ordering kept; payloads never read) | The user redirected the game's identity from twitch input to management + visible dice. Tombstones (not handler removal) keep stale clients on a structured rejection instead of a silent drop; the exact prose is part of the §7-replacement contract and is asserted in tests and acceptance. |
| 2026-07-05 | Field orientation x-MIRRORED (`FIELD.POSTS` + `FIELD.FIELDING_POSITIONS`): post 1 at (−11, 4) so runners circle counter-clockwise as seen from the match camera (screen-right = −x) | User-directed presentation choice. The mirror is symmetry-preserving — distances, angles and the slot↔post pairings are unchanged, so no formula, physics tunable or test expectation shifted except literal coordinates. |
| 2026-07-05 | New `GAME.AUTOPLAY_*` tunables (PITCH_DELAY_S 1.0, BEAT_MIN_GAP_S 0.6, TIMING_NOISE_S 0.3, RUN_BASE 0.3, RUN_NERVE_W 0.3, RUN_HELD_RISK 0.15, RUN_DIST_REF 30) — all first guesses, TUNING.md section added. **Swing sampling design:** the auto-batter's timing error is SAMPLED (uniform ±TIMING_NOISE_S) but contact is decided by the REAL `resolveSwing` window chain (reflex → CANNON_ARM mult → spin-read penalty with SWITCH immunity → pressure), with the broadcast roll's `success` recomputed from the actual contact result | Spec silent (the whole loop is new). Keeping resolveSwing authoritative means NO ability or formula regression is possible by construction — the dice only replace the human's timing input, never the resolution maths; presentation can never contradict reality. |
| 2026-07-05 | AutoPlayModule shares the room's ONE validated fielding rng stream (`options.rng`/`options.seed` → `createRng`); the draw ORDER is the code order — pitch beat draws 3 (pitch) + 2 (pre-sampled swing), then catch/fumble/run draws in tick order | One seed drives the whole match's dice (deterministic tests + acceptance). The draw-order contract is documented at the module and MUST be kept in mind when adding any new draw: inserting one shifts every later draw on the stream (the M8-era "shared stream" discipline, now covering decisions too). |
| 2026-07-05 | Client wall-clock animations (wind-up, swing, banner flash) do NOT freeze under pause — accepted existing convention | The sim (and therefore every gameplay-bearing position) freezes server-side; the client's cosmetic animations are wall-clock rAF and simply play out. Same convention as the M10 self-animating views; revisit only if a paused mid-wind-up pitcher reads as a bug in playtest. |

### 6.3 Changelog (append-only, newest first)

Entry format:

```
### YYYY-MM-DD — [Milestone N] Task title
- Changed: files/modules touched
- Verified: exact command(s) run + result (e.g. `npm run check` → 0 errors, 42 tests passed)
- Notes/deviations: anything the spec didn't cover, or "none"
```

### 2026-07-05 — [Post-ship] Auto-play redesign (Tasks 1–7, on worktree branch `worktree-autoplay-redesign`)
- Changed: shared/src/{constants,types,index}.ts + tests (T1: x-mirrored POSTS/FIELDING_POSITIONS, `RollEvent`, `GAME.AUTOPLAY_*`); server/src/modules/AutoPlayModule.ts NEW + test (T2: pure dice decisions, documented draw counts); server/src/modules/FieldingModule.ts (+`onRoll` dep) + server/src/rooms/MatchRoom.ts (T3: sim-time auto beats incl. no-contact respawn re-schedule, pre-sampled swing through the real resolveSwing chain, run beats, tombstones with exact prose, `roll` broadcasts, **drive-by REAL defect fix: stale schema ball at play boundaries — `syncBall()` after every boundary `spawnBall()`**) + MatchRoom.test.ts full 38-row migration (43 tests; rows + pinned seeds + jitter findings in `.superpowers/sdd/task-3-report.md`); client/src/RenderModule.ts (T4: BatterView with bat + wind-up/swing animations); client/src/CameraControls.ts NEW + SceneModule.ts (T5: clamped orbit camera + skybox); client/index.html + client/src/{UIModule,InputModule,NetModule,main,PositioningControls}.ts (T6: esports restyle, roll banners, play senders/keys deleted); docs/superpowers/acceptance/autoplay-{acceptance,browser-acceptance}.{mjs,txt} + autoplay-0*.png (T7); README.md (manager-not-player copy, controls table, gallery), TUNING.md (+AUTOPLAY_* section), CLAUDE.md §6 (this entry). Commits 283ea75 (T1), 8113eaf (T2), 10310b4 (T3), 5b362a9 (T4), 100058d (T5), ce176ea (T6) + this docs commit.
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 16 files / **380 passed** — run TWICE in this task, both exit 0 (test phases 839 s / 707 s; real-time beats, §6.4 runtime note; T3 additionally ran it green ×2 earlier on the branch). **Scripted WS acceptance** vs this worktree's real server (two colyseus.js clients, `autoplay-acceptance.txt`, 50 OK / 0 FAIL, exit 0): ROOM 1 seed 1 — full 24-play game to GAME_OVER (winner B 4½–1½) with ZERO play messages sent by either client (and zero rejections of any kind received); per-play roll-sequence collector green on all 24 plays (pitch first, every swing after its pitch, every re-pitch after a FAILED swing, run/catch only post-contact; 146 rolls total {pitch 41, swing 41, run 27, catch 37}); **counter-clockwise proven from the runner SCHEMA — the first runner's opening leg first ≥3 m sample at (−2.86, 1.04) and min-x −10.97 (the post-1 region); the first parked arrival also landed negative (post 2, x = −9)**. ROOM 2 — all three tombstones exact from the role that used to own the message; pause mid-play (raw socket drop): ZERO roll broadcasts across 1.2 s + ball frozen + `paused` held, reconnect with the pre-drop token → unpaused and the play resolved. **Browser acceptance** (Playwright 1.61.1/Chromium, two pages, `autoplay-browser-acceptance.txt`, exit 0): restyled lobby (code MLNH read from the DOM) + draft sheet screenshots; full table-order draft clicked through the real UI; legend carries orbit+reposition hints and NO retired play keys; orbit drag changed the render (before/after committed) and Home reset it; **reposition from the ORBITED camera accepted** ([selected] badge re-rendered off the schema patch, zero rejection feed lines, selection-ring pixels 0→13 at the click target); then hands-off watching — batter at the square pre-contact, crisp roll banners (animation-frozen clone of the real first banner — capture-lag workaround, live stack untouched), runner mid-circuit screen-right, `Carl safe at post 3 · +1½` resolved with zero client play messages, never >2 banners stacked, zero page errors both pages; second play watched close-up from the orbited camera — **wind-up frame captured (Kian mid-whip, ball just released) with the navy batter's bat visible in profile** (autoplay-05-windup-a).
- Notes/deviations: **T4 incident:** the Task-4 agent edited the MAIN repo by mistake; changes were relocated to the worktree, re-verified, main restored clean (SDD ledger row). **Port situation (harness-only):** 5173/2567 were held by a stale UNKILLABLE node pair from another session (taskkill Access denied even elevated, command lines unreadable) — the acceptance ran this worktree's identical app config on 2568 (temp `server/serve2568.tmp.ts`, deleted) + Vite 5199 with a page port-rewrite shim; both harnesses take the URL as an argument for normal `npm run dev` runs. **CCW assert re-derived honestly:** the brief's "first post ARRIVAL x < 0" is not a property the runner AI guarantees (it runs through posts 1–2 and parks at post 3, x = +3, with schema atPost −1 while running) — the committed assert pins the opening-leg DIRECTION and min-x instead, which the old clockwise field would fail by sign. Browser-harness fix found in passing: Playwright `waitForFunction(fn, {timeout})` treats the object as the fn ARG (options are the 3rd parameter) — six call sites corrected after one 30 s-default timeout blew on a miss-heavy play.
- Changed: client/src/CharacterModels.ts (NEW — 11 procedural humanoid rigs, `VISUALS` looks table, `KIT_COLOURS`, per-model ring/ball props, `setTint`, dispose); client/src/SceneModule.ts (stadium: terraced stands, 3,000-cone instanced crowd, floodlight masts, hoardings, mow-striped grass canvas with all chalk from CONST.FIELD, warm gradient sky dome + haze fog, post pennants; local `createLcg`); client/src/RenderModule.ts (rigs consumed by fielders+runners views: `setTeams` kits with in-place rebuild on kit change, holder gold ring + in-hand ball + cocked arm, selected cream pulsing ring, out topple + red `setTint`, speed-scaled limb swing + yaw + lean + idle breathing, whale swing damping, recursive-raycast `pickId`, re-skinned leather ball; all M10 out-retention/revival semantics preserved verbatim); client/src/main.ts (+4 lines: `setTeams` wiring before the `update()` calls); docs/superpowers/acceptance/{visual-acceptance.mjs, visual-acceptance.txt, visual-01-stadium.png, visual-02-lineup.png, visual-03-play.png}; README.md (gallery swap); CLAUDE.md §6. Commits d71ddf3 (T1), d818092 (T2), 38f74d5 (T3) + this docs commit. **ZERO server/shared change** (`git diff main --stat`: only the four client files + docs).
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 15 files / 357/357, exit 0 (server suite untouched; run twice green — one intermediate run hit the pre-existing WALL stop-dead timing flake now logged in §6.4, byte-identical server code to main). Browser acceptance (Playwright 1.61.1 + Chromium, TWO pages vs a real `npm run dev`, assertion-accumulating, exit 0, ~102 s; log visual-acceptance.txt): custom draft clicked through the real UI putting whale AND joe on side B (the whale is only fielded if drafted); **ANTI-PILL** — whale's rendered vertical extent 52 px vs joe's 23 px at near-equal depth (ratio 2.26 ≥ the 1.6 bar; world 3.1 m vs 1.3 m), measured by before/after screenshot diff in windows around camera-projected reposition targets ((16,26) and (0,23), Node-side camera replica); **picking regression** — panel-row select + ONE computed ground click moved each fielder (1,127/160 changed px at the targets; the m8-era 7-click convergence burst is obsolete under the M10 per-frame lerp); **stadium presence** — warm gradient sky (209,182,143 at frame top vs the old flat overcast 135,181,217) + 1,271 distinct quantised colours in the upper half (flat backdrop yields <10); **HOLDER GOLD RING captured live** (Task-3 reviewer ask): play 4, 337 gold-band px of which 321 EXACT emissive (217,164,65), new-gold centroid (535,526) = kian holding the gathered ball on the bowling square, ring clearly visible in visual-03-play.png; **out-runner topple re-verified** on the new rigs (370 red px vs 0 baseline after `caught by Kian · out: Laurie`); **both kits live** in visual-02 (navy kit-A runner among maroon kit-B fielders); **zero page/console errors** on both pages throughout.
- Notes/deviations: the brief's "positioning phase" lineup shot is impossible by design — the batting side is never rendered until a hit creates a runner — so the both-kits shot is mid-play (logged in the harness header). The gold matcher took three evidence-driven iterations, preserved honestly in the harness comments: whole-frame exact matching found ZERO px (at 30 m the 1–2 px ring antialiases entirely into grass), a whole-frame band matcher false-positived on HUD gold text antialiasing ([PRESSURE] badge + lit legend keys appearing after the baseline) and would confound with laurie's blond quiff/carl's armband — final approach is a band-delta watch confined to the bottom-centre field window (confound-free by construction) where the close-range ring renders hundreds of exact pixels. The dedicated no-swing holder drill did not trigger in-window in the passing run; the in-play watch caught the ring instead (both paths kept).

### 2026-07-04 — [Milestone 10] MERGED + TAGGED (milestone AND PROJECT BUILD ORDER closed)
- Changed: main fast-forwarded 79f4841 → 670d85e (5 commits incl. polish 670d85e); tag `m10-ui-polish`; main + tag pushed to origin; worktree + branch removed. All ten §9 milestone tags now on origin.
- Verified: `npm run check` on the branch head (polish commit) → typecheck ×3 clean, ESLint clean, 15 files / 357/357 (server/shared untouched by the whole branch — verified via diff stat at final review). Final whole-branch review "With fixes" → polish 670d85e (PRESSURE parity with RulesModule.pressure() incl. explicit tiebreak term; LEAVE-during-reconnect race guard; token cleared on markLeaving) → merged.
- Notes/deviations: the PRESSURE `* 2 - 1` formula was a PLAN defect corrected at final review (recorded in §6.2 so it is not "restored" to match the plan). Deferred/known: out-runner topple loses the schema-delete race 9/13 (client-side retain-mesh fix candidate logged); parked-runners panel filter revisit if ball-dead continuation lands.

### 2026-07-04 — [Milestone 10] UI polish: HUD, result overlay, reconnect, lobby restyle (Tasks 1–4, on worktree branch `worktree-m10-ui-polish`) — ALL SPEC §9 MILESTONES BUILT
- Changed: client/src/NetModule.ts (reconnect surface: stored `reconnectionToken`, `onUnexpectedDisconnect` (once-only across onLeave+onError, suppressed by `markLeaving`/opponentLeft), one-attempt `tryReconnect()` → fresh Net, `wrapRoom` shared by connect/reconnect); client/src/InputModule.ts (gameplay keys inert while paused); client/src/RenderModule.ts (self-animating views — per-view rAF + time-based convergence `1 − 0.001^dt`; `runners.markOut` red-topple 1.5 s retention + revival guards + dev-aid warn); client/src/UIModule.ts (NEW — scorer's board, 6-entry feed, contextual key legend with `setNotice` override, GAME_OVER result overlay with REMATCH/LEAVE; `describeCause`/`describeResolution` moved verbatim from main.ts); client/src/main.ts (rewritten `runMatch` with one idempotent `teardown()`, reconnect flow, module-level result-button wiring, feed edge events, `#status` retired); client/index.html (`#hud` + restyled lobby, `#status` removed); client/src/DraftScreen.ts (now-batting/parked info rows, full-width empty-bench note); docs/superpowers/acceptance/{m10-browser-acceptance.mjs, m10-acceptance.txt, m10-01/02/03/04-*.png}; CLAUDE.md §6. Commits f35d6b8 (T1), e60795b (T2), 722f96e (T3, incl. T1/T2 review follow-ups) + this docs commit (T4 — no src changes). Server + shared workspaces untouched.
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 15 files / **357 passed** (exit 0; identical counts to M9 — client-only milestone, client deliberately test-less per M1 precedent). §9.10 browser acceptance vs a real `npm run dev` server (two Chromium pages in SEPARATE Playwright contexts, exit 0, 109 s): restyled lobby create/join by code; full draft click-through; COMPLETE 20-play 4-innings game via REAL KEYDOWNS with per-play feed-line + both-boards-score assertions (final A 5½ – B 1½); exact role-keyed legends; result overlay (WINNER: A) + REMATCH click → zeroed fresh game; reconnect drill (forced unconsented socket drop → page 1 paused feed/notice → one-attempt auto-reconnect → `reconnected`/`game resumed` → both boards advanced to PRE_PLAY); out-runner topple pixel-verified (536 red px, visible in m10-03); zero page/console errors. Evidence: `docs/superpowers/acceptance/m10-*`.
- Notes/deviations: `UI.setNotice` addition and the now-batting row replacement (both §6.2 rows, review-upheld). `context.setOffline` documented as NOT affecting an established WebSocket in Chromium (CDP limitation) — the drill forces the drop via an init-script socket hook instead, which is also the truer test of the shipped one-attempt design (a transient drop, not a sustained outage). Task-3's open question answered: `markOut` warn does NOT spam — 9 per page over 13 outs (one per race-lost out; §6.4 note). Harness learning (no product defect): a missed swing silently respawns the ball and PLAY awaits a re-pitch (M3 design), so the harness retries the pitch/swing pair; 2 of 20 plays needed one retry. m5–m8 acceptance harnesses referencing `#status` are frozen evidence — deliberately not edited or re-run.

### 2026-07-04 — [Milestone 9] MERGED + TAGGED (milestone closed)
- Changed: main fast-forwarded 5a27a79 → 5434588 (7 commits); tag `m9-abilities`; main + tag pushed to origin; worktree + branch removed.
- Verified: `npm run check` on the branch head (fix commit) → typecheck ×3 clean, ESLint clean, 15 files / 357/357. Final whole-branch review "With fixes" (the WALL own-throw defect, empirically probed) → fix 5434588 → re-review "Ready to merge? Yes" (exemption granted only at flight start, one-way re-arm, per-tick blocker moves preserve state, whale-chase collision degenerates to spec stop-dead).
- Notes/deviations: demo 5 (live spin-read window comparison) substituted with unit-test maths — upheld by the final reviewer ("discriminating a 49.5 ms window over live WS sub-tick timing is genuinely infeasible"; the deterministic unit layer is the right home). Reviewer chain-note for M10: the blocker is armed only inside the contactMade window; any future physics-touching ability must state its arm/disarm window explicitly.

### 2026-07-04 — [Milestone 9] Final-review fixes (WALL own-throw, pitch-fact capture, shared neutral mods)
- Changed: server/src/modules/PhysicsModule.ts (**IMPORTANT fixed**: the WALL blocker ate the whale's OWN throws — a held ball parks at the holder's hands (y 1.0) INSIDE his own capsule (centre 1.3, radius 0.4) and the release pinned against it (reviewer probe + RED test: 20 m/s → ~1.8 m/s, stuck at ~0.4 m), so the whale could never effect a run-out. Fix: flight-start blocker exemption — a flight released from INSIDE a blocker's capsule disables that collider (`setEnabled(false)`) until the ball first exits its footprint (per-substep segment-distance check with a 0.05 m hysteresis guard); never granted mid-flight, so a final-approach ball is still blocked) + PhysicsModule.test.ts (+1 RED-first regression: escape at ≈ throw speed + the blocker re-arms against the NEXT inbound flight); server/test/MatchRoom.test.ts (+1 end-to-end whale-gather→throw test: whale nominated pitcher, bounced drive at him, ALWAYS_CATCH gather → hold → throw → ball escapes clean — NOT RED pre-fix: the long hold keeps the ball↔capsule contact pair alive so pre-fix escape-vs-pin was contact-event jitter; the physics test is the RED repro, this one pins the room contract); server/src/rooms/MatchRoom.ts (minor: `pitcherSpinStat` captured at handlePitch alongside `currentPitcherMods`/`lastPitchSpinInput` — swing reads the facts of the pitch it faces, robust to mid-PLAY pitcher changes); shared/src/abilities.ts (minor: neutral mod objects exported frozen as `NEUTRAL_FIELDING_PARAMS`/`NEUTRAL_PITCH_MODS`/`NEUTRAL_HIT_MODS`) with the three duplicated copies in PitchModule/HitModule/MatchRoom replaced by the shared exports (no behaviour change; neutral-equivalence tests untouched); CLAUDE.md (§6.2 WALL row addendum, §6.4 2.5–2.6 m band-edge note); TUNING.md (BUTTERFINGERS fumble dead-zone observation).
- Verified: server `npx vitest run` → 9 files / 225 passed; RED evidence for the physics regression (pre-fix: `expected 0.3198… to be greater than 3` — the pin); `npm run check` → typecheck ×3 clean, ESLint clean, 15 files / **357 passed**, exit 0.
- Notes/deviations: room-level pre-fix behaviour was jitter, not a guaranteed pin (see the test comment) — the physics probe is the canonical repro. The 2.5–2.6 m band edge and the fumble dead-zone are LOGGED (not fixed) per the review disposition.

### 2026-07-04 — [Milestone 9] Abilities wired into their modules (Tasks 1–6, on worktree branch `worktree-m9-abilities`)
- Changed: shared/src/abilities.ts (NEW registry: fielding/pitch/hit ability param derivations) + shared/test/abilities.test.ts (15 tests); shared/src/constants.ts (+`CONST.ABILITY` tunables) + constants.test.ts (+15 → 72); shared/src/formulas.ts (+`spinReadPenalty`) + formulas.test.ts (+1 → 16); shared/src/types.ts (`PitchParams` +`curveOnsetS`) + index.ts; server/src/modules/PitchModule.ts (pitch ability mods: CANNON bonus, CURVEBALL spin mult + curveOnsetS) + PitchModule.test.ts (12); server/src/modules/PhysicsModule.ts (Magnus curve-onset gate; WALL blocker stop-dead) + PhysicsModule.test.ts (34); server/src/modules/HitModule.ts (`SwingContext`: CLUTCH, power base, CANNON window, spin-read w/ SWITCH immunity) + HitModule.test.ts (18); server/src/modules/FieldingModule.ts (IMMOVABLE, BUTTERFINGERS + play-scoped `fumbledFlight` guard, LONG_REACH, POWERHOUSE, QUICK_DRAW) + FieldingModule.test.ts (47); server/src/rooms/MatchRoom.ts (ability-context threading into pitch/swing/fielding; WALL blocker armed over the contactMade window) + MatchRoom.test.ts (50); docs/superpowers/acceptance/{m9-acceptance.mjs, m9-acceptance.txt}; TUNING.md (+M9 section); CLAUDE.md §6. Commits c04f2e4 (T1), 5f731b6 (T2), 7f8d460 (T3), 5a1f3f3 (T4), 4af4518 (T5) + this docs commit (T6). Client untouched; no src changes in Task 6.
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 15 files / **355 passed** (exit 0; breakdown in §6.1). §9.9 scripted acceptance vs a real `npm run dev` server (two colyseus.js clients, assertion-accumulating harness, exit 0): CURVEBALL late-onset (z,x) profile — pre-onset |x| 0.0003 m vs post-onset 0.0367 m, > 2× dominance; WALL stop-dead at the whale (fast drive → < 0.5 m/s at d=0.11 m, y=0.02, resolved `safe`, never `caught`); IMMOVABLE guaranteed catch by jonty on a seed-only room; BUTTERFINGERS live at seed 19 (documented bounded seed search, seeds tried 7, 19) — in-flight fumble zeroed at joe's FEET y=0.04, no caught-out. ONE substituted demo, honestly logged: demo 5 (spin-read/CANNON live swing-window comparison) fell back to unit-test window maths after live sub-tick timing failed to land the 49.5 ms band.
- Notes/deviations: ZERO test re-derivations except the spec-mandated one — the M4 blocker rebound test re-derived to M9 stop-dead semantics (§6.2 row). New §6.2 rows: invented SPIN_READ_W 0.25 (USER-APPROVED), uncapped effective stats (USER-APPROVED), fumbledFlight guard, POWERHOUSE pitch-side immunity inert, WALL semantics + blocker size, the rebound-test re-derivation. New §6.4 known issue: pre-bounce catch of a THROWN ball still classifies `caught` though the struck ball had grounded (pre-existing M4-era case). **Process note (honest history):** a process crash killed the Task-6 session after the acceptance run PASSED and its files were written; docs + this commit were completed by a resumed session (acceptance evidence is pre-crash, verified intact post-crash — log complete, harness assertion structure confirmed, `npm run check` re-run green post-crash).

### 2026-07-04 — [Milestone 8] MERGED + TAGGED (milestone closed)
- Changed: main fast-forwarded 1a9797a → 7be99ec (7 commits incl. polish 7be99ec applying all six final-review minors); tag `m8-positioning`; main + tag pushed to origin; worktree + branch removed.
- Verified: `npm run check` on the branch head (polish commit) → typecheck ×3 clean, ESLint clean, 14 files / 291/291. Final whole-branch review (most-capable-model subagent) verdict "Ready to merge? Yes" — probed seams all clean: ledger × pause/disposal, positioning lifecycle × rematch, rebuilds × ready collectors, setBatter × parked runners (queue cannot summon a parked survivor), schema/view parity, §6 vs evidence.
- Notes/deviations: deferred (logged in TUNING/§6.2-§6.4): pitcher-change slot-0 overlap; ledger regen includes the active batter (no-op today); capsule lerp midway on sparse patches; subs cap only binds when a ranked mode wires SUBS_PER_INNINGS_RANKED. Reviewer note for M9: keep the "never rebuild fielding during PLAY" contract explicit around any ability work; M10 note: surface the current batter + parked runners in the batting panel.

### 2026-07-04 — [Milestone 8] Positioning, substitutions, stamina ledger (Tasks 1–5, on worktree branch `worktree-m8-positioning`)
- Changed: shared/src/types.ts (+`RepositionInput`/`SubstituteInput`/`SetBatterInput`; `FielderSetup.stamina?`) + types.test.ts (+3); shared/src/constants.ts (FIELD +`LEGAL_ZONE`/`BATTING_SQUARE_KEEPOUT`/`PITCHING_SPOT`) + constants.test.ts (+4); server/src/modules/PositioningModule.ts (NEW pure per-side positions/bench/subs with zone+keep-out validation) + PositioningModule.test.ts (5 tests); server/src/modules/RulesModule.ts (+`setNextBatter`, `queue` in view) + RulesModule.test.ts (+2); server/src/modules/FieldingModule.ts (stamina seed honoured incl. `reset()`) + FieldingModule.test.ts (+2); server/src/rooms/MatchRoom.ts (reposition/substitute/setBatter handlers per the §4 validation matrix; room-layer pitcher guard + benched-nominee rejection; `staminaById` ledger absorb→regen at endPlay top; layout-driven `rebuildFielding` with on-field `defaultPitcherFromIds`; per-innings `resetSubs`; rematch re-seed; `fieldSlotsOverride` option) + MatchState.ts (+benchA/B, subsUsedA/B, queueIds) + MatchRoom.test.ts (+6 M8 tests → 48); client/src/PositioningControls.ts (NEW raycast select/move) + DraftScreen.ts (fielding/batting panel modes) + RenderModule.ts (pickId/setSelected) + NetModule.ts (+senders/view) + main.ts (SelectionStore, subs status segment) + index.html (panel styles); docs/superpowers/acceptance/{m8-acceptance.mjs, m8-acceptance.txt, m8-browser-acceptance.mjs, m8-01/02/03-*.png}; TUNING.md (+M8 section); CLAUDE.md §6. Commits d6d880f (T1), d465e4b (T2), be3cecb+4efa5e6+e98d74d (T4), 0d2b2f7 (T3) + this docs commit.
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 14 files / **290 passed** (exit 0; breakdown in §6.1). §9.8 scripted acceptance vs a real `npm run dev` server (two colyseus.js clients, exit 0): all five reposition rejection reasons EXACT; schema move surviving into PLAY; custom layout + re-derived default pitcher returning at the innings-3 switch; real-bench room (`fieldSlotsOverride: 3`) proving substitute sync, benched-nominee rejection, bowler-subbed-out re-derivation, cross-play stamina drain (ricy 8→7.134, kian 6→5.723) + capped bench regen (min(8, 7.134+1) = 8.000 exactly), setBatter both ways, subsUsed 1→4 under the Infinity cap (log: m8-acceptance.txt). Browser acceptance (Playwright 1.61.1 + Chromium): panel-row selection + computed ground-pixel canvas click drove a real reposition, verified by pixel-sampling the capsule at the projected target (0→391 blue px) and the `[selected]` badge; empty-bench note + subs heading screenshotted; batting page's queue click announced `batter: Joe` (screenshots m8-01/02/03).
- Notes/deviations: Task-4 had one review fix round — panel-driven selection never highlighted the 3D capsule (two writers); fixed at the root by making `SelectionStore.set` the ONLY store→highlight mirror (4efa5e6). Task-3 needed ZERO re-derivations of existing tests — the default layout equals the old M7 slot map by construction, so all prior expectations held; its 6 new room tests had documented test-side idiom adaptations only (keep-out rejection-reason overlap, deterministic innings idiom, `JSON.parse('')` guards). Browser acceptance surfaced a cosmetic client quirk (single-patch lerp leaves a repositioned capsule visually midway — §6.4) and worked around it honestly with a multi-click convergence burst; no src changes made in Task 5.

### 2026-07-04 — [Milestone 7] MERGED + TAGGED (milestone closed)
- Changed: main fast-forwarded 0742b20 → 8e0e302 (7 commits incl. the pitcher-strip CSS fix 8e0e302 found by browser acceptance); tag `m7-draft`; main + tag pushed to origin; worktree + branch removed.
- Verified: `npm run check` on the branch head pre-merge → typecheck ×3 clean, ESLint clean, 13 files / 268/268 (run twice; MatchRoom.test.ts additionally 2× stable standalone). Final whole-branch review (most-capable-model subagent) verdict: "Ready to merge? Yes" — all five cross-task seams probed (draft-completion patch atomicity, setPitcher vs confirm gates, DRAFT under M6 pause, rematch squad persistence, fielder-mesh cleanup at innings switch) verified clean; all 6 test re-derivations confirmed honest.
- Notes/deviations: deferred cosmetic minors logged in §6.1. Reviewer recommendation for M8: `rebuildFielding` is the seam for custom layouts — preserve its never-in-PLAY caller contract; schedule the client-reachable `seed`-option hardening before any public play.

### 2026-07-04 — [Milestone 7] Draft (Tasks 1–5, on worktree branch `worktree-m7-draft`)
- Changed: server/src/modules/DraftModule.ts (NEW: pure alternating draft + `picksEach`) + server/test/DraftModule.test.ts (6 tests); shared/src/types.ts (+`DraftPickInput`/`SetPitcherInput`) + shared/test/types.test.ts (+2); server/src/modules/RulesModule.ts (+`completeDraft(squads)`) + RulesModule.test.ts (+2); server/src/rooms/MatchRoom.ts (real DRAFT phase — room rests in DRAFT on second join; `draftPick`/`setPitcher` handlers; `defaultPitcherId`; `rebuildFielding` on draft completion/every endPlay/rematch/setPitcher; placeholder empty FieldingModule pre-draft; mirror-nine module constants deleted) + MatchState.ts (+draftTurn/draftRemaining/squadAIds/squadBIds) + MatchRoom.test.ts (42 tests — +4 M7 draft tests, rejection matrix +draftPick/setPitcher rows, 6 test re-derivations for the 5-a-side world, see below); client/index.html (+#draft sheet styles) + client/src/DraftScreen.ts (NEW) + NetModule.ts (+draft view/senders) + main.ts (draft/bowler status segments); docs/superpowers/{specs,plans}/2026-07-04-milestone-7-draft*.md; docs/superpowers/acceptance/{m7-acceptance.mjs, m7-acceptance.txt, m7-browser-acceptance.mjs, m7-01/02/03-*.png}; CLAUDE.md §6. Commits dd3cafd (T1), 70cbbb2 (T2), fb57311+34c6ec4 (T4), 72a3a09 (T3) + this docs commit.
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 13 files / **268 passed** (exit 0; breakdown in §6.1). §9.7 scripted acceptance vs a real `npm run dev` server (two colyseus.js clients): 38/38 checks, exit 0 — full table-order draft with out-of-turn (`wrongRole`) and taken-id (prose) rejections, exact squads + whale undrafted, setPitcher accept/reject/restore with the nominee verified on the bowling square, then a full game to GAME_OVER (23 plays) asserting the fielding five + DEFAULT pitcher at every innings switch (joel for A, kian re-derived for B) and that only drafted characters batted (log: m7-acceptance.txt). Browser acceptance (Playwright 1.61.1 + Chromium): both pages clicked the ENTIRE draft through the real UI on their own turns (clicked sequence = table order), mid-draft badges, pitcher strip default kian → click Ricy → [bowling] mark moved + status `bowler: Ricy` (screenshots m7-01/02/03).
- Notes/deviations: Task-3 test re-derivations for the drafted 5-a-side world (full detail: task-3 report): (1) the innings-switch test's lofted hit re-derived to the flat-drive-at-bowler caught idiom — with 5 fielders the deep field is unmanned, so the sketch's loft parked every batter safe and the innings never drained (genuine new behaviour, not a bug; `expect(plays).toBe(5)` added); (2) the 9-fielder slot test became the drafted-five test (0 pre-draft, 5 post-draft, kian pinned to slot 0, rest in pick order); (3) the M5 "draft stub" join test now asserts the room RESTS in DRAFT; (4) caught-test membership `FIELDING_IDS`→`DRAFTED_B`; (5) M6 lobby second-join advances to DRAFT; (6) phase-walk/rejection-matrix/confirm tests gained a draft step (plumbing only). Batting order is now pick order (carl, laurie, …). The browser harness's final-pick wait needed a draft-completes-before-badge-renders branch (harness fix, not a product defect). One cosmetic client defect found and LOGGED (§6.4): pitcher-strip rows use the `hidden` attribute but `.draft-row`'s `display:grid` overrides it visually. `npm audit` unchanged. No tunables emerged (TUNING.md untouched).

### 2026-07-04 — [Milestone 6] Two-player sync (Tasks 1–5, on worktree branch `worktree-m6-two-player-sync`)
- Changed: server/src/rooms/MatchRoom.ts (seats + sideOf, `code`/`reconnectGraceS` options, role gating with `wrongRole`/`paused` reasons, both-side confirm/ready gates, paused tick freeze, onLeave consented/drop branches + `shuttingDown` guard), MatchState.ts (+roomCode/sessionA/B/connectedA/B/paused), app.config.ts (`filterBy(['code'])`); shared/src/constants.ts (+`GAME.RECONNECT_GRACE_S`) + constants.test.ts; client/index.html (lobby overlay) + src/{NetModule,InputModule,main}.ts (create/join by code, mySide/myRole, detachable role-gated input, pause/opponent-left states); server/test/MatchRoom.test.ts (+11 M6 tests); docs/superpowers/acceptance/{m6-acceptance.mjs, m6-acceptance.txt, m6-browser-acceptance.mjs, m6-01/02/03-*.png}; TUNING.md (+RECONNECT_GRACE_S); CLAUDE.md §6. Commits fe01cd5, 92ede9c, 01e50f0, 016b5ee, a794fdd, 094606c, 42c3243, b5608ca + this docs commit.
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 12 files / **253 passed** (exit 0). §9.6 scripted acceptance vs a real `npm run dev` server (two colyseus.js clients, every message from its role-correct client): create-by-code MSIX + join-by-code into the same room; full game to GAME_OVER (95 plays, 3 innings switches, caught 30/runOut 7/safe 58, winner A 51½–0½); deliberate wrong-role pitch → structured `wrongRole` rejection; consented quit → `opponentLeft {side:'B'}` + disposal; second room: raw-WS drop (`room.connection.transport.ws.close()`, works as-is on colyseus.js 0.15.28) → `paused=true`, ball x/z IDENTICAL across 1.2 s, `paused` rejection, `client.reconnect(token)` → resumed (log: m6-acceptance.txt). Browser acceptance (Playwright 1.61.1 + Chromium, THIS Windows machine): Create displays code NQPK in the overlay, joiner enters it, both pages reach INITIAL_POSITIONING (m6-01/02/03 screenshots).
- Notes/deviations: **the browser acceptance caught a real Task-4 client defect** — `runMatch()` unconditionally hid the lobby overlay immediately after `startCreate()` showed it (and the code was read from pre-sync state, so blank even if visible): the creator could NEVER see the room code, making code matchmaking unusable through the real UI while all committed tests stayed green (they exercised NetModule/InputModule seams, not main.ts choreography). Fixed at b5608ca (conditional phase-based hide is the single hide path; `#lobby-code` driven from synced state) and re-verified live. Task-4 fix rounds during the milestone: opponentLeft client handling + listener leak (01e50f0, 016b5ee — one live keydown handler per match); Task-3 fix round: onLeave re-entrancy guard + fire-and-forget disconnect (42c3243, §6.2 row). Latency compensation deliberately NOT taken up (user choice, §6.4 update). Playwright+Chromium now WORKS on this Windows machine (`npx playwright install chromium`), superseding the M4-era headless-Edge blocker note for THIS machine too; playwright is not a repo dependency (harness header documents how to run it). `*.log` is gitignored, so the acceptance log is committed as `.txt` (M5 precedent).

### 2026-07-04 — [Milestone 5] INTEGRATED: bundle verified, main fast-forwarded, tags pushed, stale branch removed
- Changed: repo refs only, no source changes — main 5a1d112 → bcce53d (pure fast-forward from `carlquest3m5complete.bundle` branch `claude/worktree-completion-merge-bvswsy`); main + branch + all 5 milestone tags pushed to origin; `worktree-m5-rules-engine` deleted locally and on origin; worktree dir `.claude/worktrees/m5-rules-engine` removed; `npm install` package-lock libc-metadata churn discarded (same class as the M4 precedent).
- Verified: `git bundle verify` → "records a complete history"; pre-push on the bundle branch: `npm run check` → typecheck ×3 clean, ESLint clean, 12 files / **241 passed** (241); worktree head 97824d9 confirmed ancestor of bcce53d (`git merge-base --is-ancestor`) before deletion; `git status` clean post-integration.
- Notes/deviations: none — integration only, nothing re-implemented.

### 2026-07-04 — [Milestone 5] Client + acceptance + docs (Tasks 6–7, milestone closed on branch)
- Changed: client/src/{NetModule,InputModule,RenderModule,main}.ts (M5 state view incl. runners MapSchema + rules mirror; confirmPositioning/readyForPlay/rematch senders; playOutcome carries PlayResolution; rejected handler; Enter confirm/ready + N rematch keys; per-id reconciled runners view replacing the single-runner capsule; phase/score/innings/outs/batter status line + resolution summary); eslint.config.js (+docs/superpowers/acceptance ignore); docs/superpowers/acceptance/ (new: m5-acceptance.mjs harness, game + tiebreak logs, 3 browser screenshots); TUNING.md (M5 rounder-unreachable note); CLAUDE.md §6.
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 12 files / 241 tests passed. §9.5 acceptance vs a real `npm run dev` server (colyseus.js over ws://localhost:2567): mode `game` seed 42 → 76 plays, 3 innings switches, caught 31 / runOut 5 / safe 40, out-of-phase pitch rejected (structured broadcast), GAME_OVER winner A 29½–1½, rematch reset verified; mode `tiebreak` seed 7 → genuine 0–0 after both innings pairs, sudden-death pair A 1½ – B 0 → winner A. Browser (Playwright/Chromium, real keydowns): join → INITIAL_POSITIONING status line → Enter → Enter → P → Space → `last: Carl safe at post 3 · +1½`; screenshots committed.
- Notes/deviations: live full rounder unreachable with current tunables (circuit ~47.4 m vs PLAY_TIMEOUT_S 6) — logged in TUNING.md, covered by RulesModule unit tests; acceptance therefore demonstrates caught/runOut/safe/half-rounder (the plan asked for a rounder). Client remains test-less by design (M1/M3/M4 parity). Browser-capture blocker from M4 §6.4 removed (new Linux environment).

### 2026-07-04 — [Milestone 5] Rules engine server layer (Tasks 1–5, via worktree branch)
- Changed: shared/src/types.ts (+TeamSide/PlayResolution/SettlementFact; runnerId on runOut/safe) + shared/test/types.test.ts; server/src/modules/RunningModule.ts (multi-runner rewrite: shared stop/go, forced-on cascades, exposures(), markOut(id), settlePlay() with collision-free parking) + tests; server/src/modules/RulesModule.ts (new pure state machine: innings, scoring, batting queue, tiebreak, pressure()) + tests; server/src/modules/{HitModule,FieldingModule}.ts (pressureMult wiring) + tests; server/src/rooms/{MatchRoom,MatchState}.ts (real phase machine, message validation matrix + structured `rejected`, rules-mirror schema, per-runner exposure-scoped run-out, PlayResolution in lastOutcome) + major MatchRoom.test.ts migration. Commits 394c594, 2b91332, 29b5a07, 5f0d5ce, 97824d9 (authored in the user's worktree session on `worktree-m5-rules-engine`).
- Verified: (this session, fresh clone) `npm install` + `npm run check` → typecheck ×3 clean, ESLint clean, 12 files / 241 tests passed, including the full-headless-game-to-GAME_OVER and rematch marquee tests and all preserved M3/M4 regression classes (garbage join options, throw pipeline, stale-crossing variants).
- Notes/deviations: MatchRoom.test.ts documents (line ~570) that a live rounder is unreachable — the M5 room test for +2-halves scoring is deliberately absent; RulesModule covers it. demoLog removed (structured `rejected` replaces it — stale §6.4 item dropped).

### 2026-07-03 — [Milestone 5] Tasks 1–4 landed on worktree branch; Task 5 implemented, uncommitted (session handover)
- Changed (branch `worktree-m5-rules-engine`): shared/src/types.ts (+TeamSide, PlayResolution, SettlementFact, runnerId on runOut/safe) + types.test.ts (394c594); server RunningModule multi-runner rewrite — N runners, shared stop/go, forced-on cascade, exposures(), settlePlay() parking, 24 tests (2b91332); NEW server/src/modules/RulesModule.ts — pure §2 phase machine, innings A,B,A,B, half-rounder scoring, sudden-death tiebreak, 22 tests (29b5a07); Hit/FieldingModule pressureMult wiring, optional no-pressure defaults (5f0d5ce). UNCOMMITTED working-tree: Task 5 MatchRoom/MatchState/MatchRoom.test.ts (phase machine, structured `rejected` broadcast replacing demoLog, multi-runner run-out wiring) — NOT yet verified.
- Verified: Tasks 1–4 each review-Approved (subagent reviews; minors logged in the SDD ledger, none blocking); branch `npm run check` 234/234 at 5f0d5ce. Task 5 has NO verification evidence yet — treat as unverified code.
- Notes/deviations: parallel-agent convention this session (user-directed): concurrent implementers do not commit; controller serialises commits per task. Tasks 3+4 ran concurrently with each other, then reviews 3+4 ran concurrently with the Task 5 implementer. Worktree was created off stale origin/main and hard-reset to local main d6fc528 before work (EnterWorktree baseRef caveat — check `git log` after creating worktrees). [Merged from main's handover commit; Task 5 was subsequently committed as 97824d9 and verified — see the 2026-07-04 entries above.]

### 2026-07-03 — [Milestone 4] MERGED + TAGGED (milestone closed)
- Changed: main fast-forwarded 97a9cc0 → c3f2457 (7 commits: Task 5 wiring a1cd756 + run-out fixes 8b2049b/99bbb8a, Task 6 client 7377153, Task 7 docs 6615a56, final-review fixes cbb70d0/c3f2457); tag `m4-fielding-running` at c3f2457; worktree + branch removed.
- Verified: `npm run check` on merged main → typecheck ×3 clean, ESLint clean, 191/191 tests. Final whole-branch review (senior-reviewer subagent, 2 fix rounds) closing verdict: "Ready to merge? Yes".
- Notes/deviations: tags m1/m2/m3 found missing from this clone and recreated from linear history (see §6.1 tag note). Browser visual acceptance remains uncaptured (headless Edge broken in this environment, §6.4); all §9.4 behavioural acceptance proven over real WS.

### 2026-07-03 — [Milestone 4] Final-review fix round 2 (snapshot exposure-window guard)
- Changed: server/src/rooms/MatchRoom.ts (the minor-3 pre-fielding snapshot is now also guarded by `preExposed === lastExposedPost`, compared at the snapshot site before checkRunOut overwrites it); server/test/MatchRoom.test.ts (+1 regression test: while-halted stale crossing + between-tick go, plus a roomRef late-binding comment on the throw test); CLAUDE.md §6.4 (snapshot scoping recorded so the condition isn't "simplified" away).
- Verified: RED first — pre-fix the new test failed with the wrongful `runOut` exactly per the reviewer's trace (runner halted at post 1, weak hit rolled through post 2's sensor while halted, go:true between ticks → snapshot read the stale latch before the transition-clear). GREEN post-fix; `npm run check` → typecheck ×3 clean, ESLint clean, 10 files / **191 passed**; MatchRoom.test.ts stability: 9/10 on an un-instrumented loop (one transient, output lost), then 12/12 (new test alone) and 10/10 (full file, capture enabled) — 19/20 full-suite aggregate, lone failure never reproduced (details in task-7-report Round 2).
- Notes/deviations: the regression test's repro vehicle is a deliberately weak (~9-tick-late) swing so the ball reaches post 2 only after the 1.84 s halt; the slowest sub-tick jitter band can time the play out first, degrading to a vacuous-but-green pass of the same assertion (documented in the test; the RED run confirms the repro band is what actually executes).

### 2026-07-03 — [Milestone 4] Final-review fix round (Importants 1–2, Minors 3–5)
- Changed: server/src/rooms/MatchRoom.ts (runtime-validated join options; pre-fielding `wasBallAtPost` snapshot ORed into the run-out check while exposure unchanged); server/src/modules/FieldingModule.ts (**real bug fixed**: `hasBounced()` is now read BEFORE `holdBallAt()` — the room binds holdBallAt to `physics.spawnBall`, whose `placeBall` resets the bounce flag, so every post-bounce pickup had been misclassified as a pre-bounce `caught`, i.e. a wrongful out; surfaced by the new end-to-end throw test, exactly the coverage gap Important 2 flagged); server/test/MatchRoom.test.ts (+2 tests: full throw→run-out pipeline, garbage rng/seed join options; stale retry comment fixed); server/test/FieldingModule.test.ts (+1 regression test coupling the stubs like the real physics); CLAUDE.md (§6.2 correction row, §6.4 markOut/snapshot notes); .superpowers/sdd/task-7-report.md (honest §2(a) heading + fix-round appendix).
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 10 files / **190 passed** (187 prior + 3 new; all prior tests unchanged and green). TDD RED evidence: garbage-options test failed pre-fix with 2× `deps.rng is not a function` uncaught-exception logs; FieldingModule order test failed pre-fix with `caught` instead of `gathered`; throw-pipeline test failed pre-fix with outcome `caught by kian` (the misclassification bug) instead of a gather→throw→`runOut`. 10× MatchRoom.test.ts stability loop: see task-7-report fix-round appendix for the tally.
- Notes/deviations: `markOut` deliberately still not called (not observable given endPlay→reset→sync ordering — §6.4). The reviewer's "permanent soft-lock" severity on Important 1 was overstated (one throw per radius entry, since the entry latch is set before the roll) but the finding itself was valid and is fixed. Acceptance scenario (a) is now honestly labelled a hit-ball run-out; the gather→throw→run-out pipeline is covered by the deterministic room-level integration test instead.

### 2026-07-03 — [Milestone 4] MatchRoom wiring, client rendering, acceptance (Tasks 5–7)
- Changed: server/src/rooms/{MatchRoom,MatchState}.ts (fielding/running wiring, FielderSchema/RunnerSchema, runDecision, event-accurate exposure-scoped run-out, outcome resolution + playOutcome broadcast, `{seed?,rng?}` options), server/src/modules/PhysicsModule.ts (wasBallAtPost/isBallAtPost/clearPostCrossings via post-sensor COLLISION_EVENTS) + PhysicsModule/MatchRoom tests; client/src/{RenderModule,NetModule,InputModule,main}.ts (fielder + runner capsules, R/T run keys, playOutcome status line); TUNING.md (new, repo root); CLAUDE.md §6. Commits a1cd756, 8b2049b, 99bbb8a (run-out fixes), 7377153 (client).
- Verified: `npm run check` on `worktree-m4-fielding-running` → typecheck ×3 clean, ESLint clean, `npm run test` 10 files / 187 passed (86 shared, 101 server). §9.4 acceptance over a REAL Colyseus server (scripted colyseus.js WS client vs `npm run dev` on ws://localhost:2567), all three outcomes captured as live `playOutcome` broadcasts: (a) seed 5 flat hit @post1, running → `{"kind":"runOut","atPost":1}`; (b) SAME seed 5, lofted mid-field hit + `runDecision{go:false}` → `{"kind":"safe","atPost":1}`; (c) seed 1 flat hit @bowler → `{"kind":"caught","by":"kian"}`. Vite 200 on 5173, Colyseus on 2567. Harness + logs: `.superpowers/sdd/acceptance.mjs`, `acceptance-output.log`, `task-7-report.md`.
- Notes/deviations: new §6.2 rows (demo fielding-side mapping; safe-at-previous-post; rng seed source). New §6.4 items (any-client runDecision; JSON-string lastOutcome; static runner stamina; one-tick exposure-transition simultaneity; headless-Edge screenshot could NOT be captured this session). Browser visual acceptance NOT captured: headless Edge exits code 13 (fails to launch headless) in this environment even for a trivial page — see §6.4; client rendering verified by code + served page instead. No src changes made in Task 7 (acceptance found no defect).

### 2026-07-03 — [Milestone 4] Fielding/Running server layer (Tasks 1–4, via bundle)
- Changed: shared/src/{types,constants,formulas,index}.ts, shared/src/rng.ts (new) + 3 test files; server/src/modules/PhysicsModule.ts (hasBounced via EventQueue, setBlocker/clearBlocker), server/src/modules/{FieldingModule,RunningModule}.ts (new) + tests; docs/superpowers/{plans,specs} M4 documents. Source: `carlquest3m4.bundle` (external Claude session), branch fast-forward-merged to main at 97eb82c.
- Verified: `npm run check` on the bundle branch pre-merge → typecheck ×3 clean, ESLint clean, 175/175 tests (10 files). `git bundle verify` OK; base commit = prior main head, so a pure fast-forward. `package-lock.json` churn from local `npm install` (libc metadata) discarded.
- Notes/deviations: Tasks 5–7 (MatchRoom wiring, client, acceptance) NOT included — milestone still open, no m4 tag. New §6.2 rows: RunDecisionInput, kinematic fielders, M4 tunables, seeded RNG, neutral ability hooks.

### 2026-07-03 — [Milestone 3] Pitch/Hit single-player loop (Tasks 1–6)
- Changed: shared/src/{formulas,characters}.ts (new) + types/constants/index + 3 test files; server/src/modules/{PitchModule,HitModule}.ts (new) + tests; server/src/rooms/{MatchState,MatchRoom}.ts (demo loop) + integration tests; client/src/{NetModule,RenderModule,InputModule}.ts (new), main.ts, index.html, package.json (+colyseus.js); eslint.config.js (ignore scratch dirs).
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 112/112 tests. Acceptance (spec §9.3): scripted Colyseus client over real WS — pitch curved via Magnus (x −0.13 by the plane), swing at z=0.51 connected timing factor 0.99, exit 33.3 m/s = exitVelocity(8, 0.99) exactly; in-browser per-frame canvas scan proved the rendered ball tracks the authoritative trajectory (35 frames, 11→62 px). Server integration test proves the loop headlessly.
- Notes/deviations: see new §6.2 rows (aim-based launch user-approved; swing.timing ignored; sidespin mapping; demo cast; degenerate-aim guards). Review process caught a real NaN-injection bug (vertical aim) pre-merge; fixed TDD in both modules.

### 2026-07-03 — [Milestone 2] PhysicsModule (Tasks 1–5)
- Changed: shared/src/types.ts (+PitchParams/HitParams/BallState), shared/src/constants.ts (+3 tunables), shared/test/constants.test.ts (39 tests); server/package.json (+@dimforge/rapier3d-compat@0.14.0); server/src/modules/PhysicsModule.ts (new); server/test/PhysicsModule.test.ts (16 tests).
- Verified: `npm run check` → typecheck ×3 clean, ESLint clean, 57/57 tests. §9.2 acceptance: spun pitch deviates >0.1 m laterally vs spinless (which drifts <1e-6); opposite spin curves opposite; bounce apex ratio in (0.05, 0.4); damping bleeds speed and spin; determinism = exact float equality over 300 substeps between twin modules.
- Notes/deviations: restitution combine rule Max (see §6.2); `spawnBallAt` free function instead of `this.spawnBall` in the factory literal (strict-mode `this` typing); each task independently reviewed (all approved, 0 critical/important findings).

### 2026-07-03 — [Milestone 1] Monorepo scaffold (Tasks 1–5)
- Changed: root workspace (package.json, tsconfig.base.json, eslint.config.js, .prettierrc.json, vitest.workspace.ts, README.md, .gitignore); shared/ (package.json, tsconfig.json, src/{index,types,constants}.ts, test/constants.test.ts); server/ (package.json, tsconfig.json, vitest.config.ts, src/{index,app.config}.ts, src/rooms/{MatchRoom,MatchState}.ts, test/MatchRoom.test.ts); client/ (package.json, tsconfig.json, vite.config.ts, index.html, src/{main,SceneModule}.ts); .claude/launch.json; plan doc amendments.
- Verified: `npm run check` → typecheck clean ×3 workspaces, ESLint clean, 38/38 Vitest tests passed. `npm run dev` → Vite HTTP 200 on 5173 AND Colyseus listening on 2567 concurrently. Visual acceptance: headless-Edge screenshot of http://localhost:5173 shows ground, batting/bowling squares, 4 posts, sky. Server boot test joins a client and reads phase 'LOBBY' from synced schema.
- Notes/deviations: see Decisions Record 2026-07-03 rows (typescript-eslint v8, Bundler resolution kept, plain ConfigOptions export, vitest threads pool). Node.js 24 LTS was installed on this machine via winget (was absent). Executed via subagent-driven development; each task passed an independent spec+quality review.

### 6.4 Known Issues (keep current — remove only when fixed and verified)

- Known-flake candidate (M6 final-review fix round): `MatchRoom.test.ts` › `matches a filtered join to the room with that code, and rejects a wrong code` once exceeded its bare 5 s default timeout under full-suite load (isolated run 85 ms; clean on rerun). Pre-existing matchmaker-latency coupling, not caused by the M6 changes; give it an explicit timeout if it flakes again.
- ~~Known-flake candidate (visual-overhaul T4): the WALL stop-dead room test~~ **DEAD — fixed at root by the 2026-07-05 auto-play redesign** (T3): the flake class was real-time swing-MESSAGE landing jitter, which no longer exists — swing timing is a sampled value applied at the deterministic plane crossing. The re-derived WALL tests (whale parked on the pinned seed's own play-1 hit corridor) ran 5/5 + 1 full-suite standalone-stable with zero retries (T3 report §5).
- **The `checkRunOut` exposure-window guards LOST their room-level regression tests in the auto-play redesign (T3 review carry-forward — the §6.2 2026-07-03 exposure-scoping rows and the do-not-simplify warning in the one-tick-simultaneity item below now stand WITHOUT their old test coverage).** The two stale-crossing regression tests (halted-runner crossing not cashed on a later go; snapshot honoured only across an unchanged window) were RETIRED with the wire seam: their choreography needed a player-timed BETWEEN-TICK `runDecision` over a live ball, and auto run beats apply decisions INSIDE the tick after the run-out check. But the guard code is UNCHANGED and REMAINS LOAD-BEARING under multi-runner arrival beats — a shared go decision can still un-halt a runner over a stale latch between successive checks — so do not treat the missing tests as evidence the guards are dead code, and do NOT simplify the `preExposed`/`lastExposedPosts` conditions. Named follow-up (unscheduled): either a test-only decision-injection room option (the `seed`/`fieldSlotsOverride` precedent) to reconstruct the choreography, or extract the snapshot-guard logic into a pure unit-testable function. Until then the guards are exercised (not asserted) every play, plus the PhysicsModule latch unit tests.
- Suite runtime grew to ~11–13 minutes (`npm run check`): the migrated room tests watch REAL-TIME auto-play beats (two full-game marquees at ~2–4 min each). Deliberate — the beats are the product now; if the wall becomes painful, the named lever is a test-only beat-pacing override option, not shorter games.
- Server test stdout is not pristine: `@colyseus/tools` prints an ".env file not found" info line, and MatchRoom onJoin/onLeave console.logs (plan-mandated) appear in Vitest output. Cosmetic; revisit when MatchRoom gains real logic.
- `npm audit` reports 10 vulnerabilities (8 moderate, 1 high, 1 critical) as of 2026-07-03, triaged at M1 final review: critical = Vitest UI file-read/execute (UI never used, dev-only); high = Vite dev-server issues (localhost dev only); moderates = esbuild dev CORS + nanoid <3.3.8 in @colyseus/core (only runtime one; predictability condition doesn't apply to Colyseus usage). All fixes need breaking major bumps (Vite 7/8, Vitest 3, Colyseus 0.16+) — deferred deliberately; revisit at a natural upgrade point.
- Client `SceneModule.resize()` re-applies `setPixelRatio(window.devicePixelRatio)` per resize event — reviewed and kept (catches cross-monitor DPI changes; idempotent, cheap). Not a defect; noted so it isn't re-flagged.
- `server/package.json` `build` script aliases typecheck (no emit config yet); real build wiring deferred until a milestone needs emitted server JS.
- Client WebGL renders very slowly in the sandboxed preview browser (software rasteriser) — verification used headless Edge instead; real browsers are fine.
- `isBallAtPost` remains a discrete end-of-tick intersection poll, now used in run-out checks for the resting-inside-the-sensor case (entry events don't re-fire when an exposure window opens) plus instantaneous queries. Fly-through detection is `wasBallAtPost(n)`: per-substep `EventQueue` sensor-intersection latches (reset on spawn/pitch/hit AND cleared by MatchRoom on every `exposedPost()` change, so a crossing only counts while the runner is actually exposed to that post — Task-5 fix round 2), capturing an intra-tick fly-through the once-per-tick poll misses (Task-5 review Important 1). This is the "switch to EventQueue rather than widen the sensor" path this note previously flagged; the earlier `sweptPost` proximity heuristic in MatchRoom has been removed. Note: `wasBallAtPost` catches intra-tick crossings but NOT a crossing that skips every fixed substep pose (sensors get no CCD); that is irrelevant to M4 run-outs (balls are delivered TO posts at post-bounce/rolling speeds).
- ~~Swing-vs-plane-crossing timing ambiguity / latency-compensation tuning item~~ **MOOT since the 2026-07-05 auto-play redesign:** player swings no longer exist, so network latency can never touch swing timing — the batter's timing error is a server-side sampled value applied at the plane crossing through `resolveSwing`. (The one-tick crossing-detection granularity is still there mechanically but carries no fairness meaning with no human in the loop.) The M6-era "revisit remote-play fairness" item is closed.
- Contact has no lateral/height proximity requirement — a swing "connects" wherever the ball crosses the z-plane (spec §5 defines contact purely by timing). Revisit with Fielding/Rules (M4/M5).
- Room CREATION options `seed` and `reconnectGraceS` remain wire-reachable from the creating client (runtime-validated, junk falls back to defaults — §6.2). The M3–M5 "no per-client role gating" item is FIXED in M6 (seats + `sideOf` gate every message; verified in tests and live acceptance), but a creator supplying a numeric `seed` still gets predictable catch rolls. M7 (draft/roles) did NOT take option gating up — it remains open; drop or gate the options outside test transports at a hardening pass.
- Bench and substitutions remain structurally EMPTY in LIVE play at the current 11-character roster: `picksEach` caps at `floor(11/2)` = 5 picks each, below the 9 field slots, so no drafted character ever sits out and the bench panel shows `bench — awaiting roster growth`. The full bench/substitution/regen MACHINERY landed in M8 and is exercised end-to-end via the test-only `fieldSlotsOverride` room option (§6.2 row) — unit tests, room tests and the live acceptance all run real benches; only the default-roster game can't reach them. Not a defect, a roster-size consequence (§6.2 M7 sizing row); resolves itself with roster growth.
- Huge-but-finite aim components (~1e308) overflow normalisation to a zero-velocity pitch/hit (self-inflicted only, no NaN); pre-scale by max component if it ever matters.
- Tuning note (TUNING candidate): a max-power 60°-elevation hit flies ~6.0 s vs PLAY_TIMEOUT_S = 6 — the ball can be despawned mid-air at the extreme.
- The ball never sleeps: Magnus `resetForces`/`addForce` wakes it every substep even at rest. Harmless at one body / 60 Hz; skip near-zero forces if it ever matters.
- `MatchState.lastOutcome` (and `lastRejection`) are JSON-serialised strings (carrying the full `PlayResolution`) rather than structured `@type` schemas — schema numbers can't be `null` and the M5 shape is a discriminated union. The M10 UI binds to outcomes via the `playOutcome`/`rejected` BROADCASTS (typed payloads, no parsing of the mirror strings needed) and this works fine end-to-end; the string mirrors remain for tests/observability. A structured schema is now only a nice-to-have if outcome fields ever need per-field patch semantics.
- FIELDER fatigue now DOES carry across plays (M8 ledger — the fielder half of the old M4/M5 note is FIXED and verified live: sprint/throw drains persist into the next play's setup, benched characters regain `BENCH_STAMINA_REGEN` capped at stat, rematch re-seeds). What REMAINS: `RunningModule` still fixes each runner's speed at `startRun` from full-stamina `fatigueMult` — running never tires anyone and a drained fielder who comes up to bat runs at full speed. M9 (abilities) did NOT take runner fatigue up — it remains a tuning-pass item; also revisit the ledger's regen scope then (it currently regens the ACTIVE batter too — a no-op precisely because runner speed ignores the ledger; §6.2 row).
- Pre-bounce catch of a THROWN ball still classifies `caught` although the struck ball had grounded (pre-existing M4-era case, flagged at the M9 Task-4 review): FieldingModule's caught/gathered split keys on `hasBounced()` for the CURRENT flight, and a throw is a fresh flight — so a fielder plucking a thrown ball out of the air before it bounces registers a `caught` out even though the batter's hit had already hit the ground. The M9 `fumbledFlight` guard covers fumbled flights only, not thrown ones. Fix candidate: a flight-kind tag set on `applyThrow` (thrown flights can never be `caught`); revisit at the next fielding pass.
- WALL 2.5–2.6 m band edge (M9 final review, same family as the thrown-ball note above): the whale's blocker capsule tops out at 2.6 m but `CATCH_HEIGHT_MAX` is 2.5 m — a ball clipping the capsule in that 0.1 m band is stopped dead ABOVE catchable height, then drops into the whale's radius as a fresh slow approach and rolls a NEW catch attempt: a manufactured out from a ball that was never catchable in flight. Narrow (0.1 m at one x/z annulus) and unobserved live; fold into the same flight-kind-tag fix candidate (tag the stopped-dead flight so its subsequent pickup classifies gathered, mirroring the thrown/fumbled cases).
- One-tick simultaneity at exposure transitions (M4, generalised in M5 to exposure SETS — `lastExposedPosts` + per-post `crossedSnapshot` in MatchRoom keep the same guard discipline): if the ball crosses an exposed post's sensor and the runner arrives at that post in the same tick, `running.tick` changes the exposure set before `checkRunOut` reads the latch — the photo-finish resolves in the runner's favour. Same error class as the logged swing-vs-plane same-tick ambiguity; M6 kept server-authoritative timing (no latency work), so this joins the same post-M6 "revisit if remote play feels unfair" tuning item. The RELATED same-tick case — ball crosses the exposed post's sensor and is gathered in the SAME tick (holdBallAt → placeBall clears the latches before checkRunOut reads them) — IS now handled: MatchRoom snapshots `wasBallAtPost(exposed−1)` right after `physics.step`, before `fielding.tick`, and ORs it into the run-out check (final-review minor 3). **The snapshot is DELIBERATELY scoped to an exposure window unchanged since the last check AND across the tick (`preExposed === lastExposedPost` at the snapshot site, `exposed === preExposed` in checkRunOut) — do not "simplify" either condition away:** exposure can change between successive checks when a go decision un-halts a runner (pre-redesign: a between-tick player runDecision; since the 2026-07-05 redesign: an auto run beat — the class survives), and on the first tick of that window the latch can hold a crossing that PREDATES it (ball rolled through the post while the runner sat halted; nothing clears latches while exposure is null) — honouring the snapshot there resurrects the stale-crossing false run-out (M4 final-review round 2). **The room-level regression tests that pinned this (`…HALTED does not run them out on the later go` and its sibling) were RETIRED with the play-message wire seam — see the dedicated coverage-gap item above; the warning stands WITHOUT test cover.** No deterministic room-level reproduction was constructible for the same-tick gather-erasure coincidence itself (it needs a sensor crossing and a catch in one 16.7 ms tick with a scripted-win roll), so that half is covered by the snapshot's code path running in every play plus all run-out tests staying green, not by a dedicated test.
- Out-runner topple vs the schema-delete race (M10 — supersedes the old "out runner simply vanishes" item; cosmetic): out PRESENTATION landed in M10 — `main.ts` calls `runners.markOut(id)` per `playOutcome.outs` entry and the runner figure tints red, topples 90° and is retained 1.5 s past its schema delete (pixel-VERIFIED live in the M10 acceptance on the then-capsules, visible in m10-03, and RE-verified on the visual-overhaul rigs in the T4 acceptance). But `endPlay` still deletes settled runners from the schema in the same tick as the broadcast, so whether the mesh still exists when `markOut` runs is a patch-vs-message arrival race: when the delete patch wins, `markOut` logs its bounded dev-aid console.warn and that runner still just vanishes (9 of 13 outs in the acceptance game; the run-out and 3 catches DID topple). `RunnerSchema.out = true` remains unobservable over the wire (M5 note) — the authoritative fact always reaches clients in `playOutcome.outs`. Fix candidates if the topple should land every time: broadcast-then-settle on the server, or delay the schema delete one patch. **Client-side fix candidate (added at M10 polish final review, alongside the two server-side ones above):** have `RenderModule` itself retain a schema-deleted runner's LAST-KNOWN mesh/position for `RUNNER_OUT_RETAIN_MS` before actually removing it, rather than removing it the instant the id disappears from the MapSchema — so a `markOut` that arrives after the delete patch still finds a mesh to topple instead of losing the race outright.
- Browser capture environment note: Playwright + Chromium WORKS on the current Windows machine too (M6 acceptance: `npm i playwright` in a scratch dir + `npx playwright install chromium`, then headless capture including DOM reads and clicks) — this supersedes the M4-era headless-Edge blocker (exit code 13) recorded for this machine; the M5 Linux-environment note stands as history. WebGL runs on a software rasteriser under headless Chromium — fine for screenshots, not for performance judgements. Playwright is deliberately NOT a repo dependency (see the m6-browser-acceptance.mjs header for how to run it).
- ~~Client reconnect gap~~ **FIXED in M10 and drilled live** (the M6 final-review Important 2 item is closed): NetModule now stores `reconnectionToken`, every unexpected disconnect (onLeave AND onError, once-only) triggers one `tryReconnect()` attempt before falling back to the lobby, and the catch-all path detaches input/tears down cleanly either way — proven end-to-end in the M10 browser acceptance (forced unconsented drop → opponent paused → auto-reconnect → both pages live and advancing). Residual scope, BY DESIGN (§6.2 row): the attempt is a single immediate one — a drop during a SUSTAINED outage lands in the lobby with `connection lost` rather than retrying through the server's 60 s grace; the token is module-level (in-memory), so a full page reload still cannot rejoin. Revisit (retry/backoff + sessionStorage token) only if real remote play wants it.
- Focused-panel-row Enter re-activation (surfaced by keyboard-driven harnesses in the visual-overhaul T3 smoke and kept in mind by T4; cosmetic, pre-existing M8/M10 DOM behaviour, NOT introduced by the overhaul): after clicking a draft/panel row, the `<button>` keeps focus, so a later Enter keydown (e.g. confirm/ready) natively re-activates the row — re-selecting a fielder or re-picking. Harnesses blur the active element before pressing Enter; a real player clicking then pressing Enter can trigger it too. Fix candidates for a future UI pass: blur rows after click, or ignore synthetic keyboard clicks on rows.
- **Reviewer's post-project note (M10 polish, final review):** the batting panel's parked-runners filter (`DraftScreen.ts`, `state.runners` filtered to `atPost >= 1 && !out && !running`) is correct ONLY because a parked survivor is currently the sole way a runner can sit in the schema between plays without being the current batter or mid-run — it works by construction, not because the filter models "parked" directly. If ball-dead continuation ever lands (a standing M5 scope note: "play still ends at first outcome"), a runner could be stationary-but-not-parked MID-play (e.g. paused between bounces of a still-live ball), and this filter would need an explicit parked/settled flag rather than inferring it from atPost/out/running. Revisit then; not a defect against the current single-outcome-per-play model.
